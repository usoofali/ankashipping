import {
  __privateAdd,
  __privateGet,
  __privateMethod,
  __privateSet,
  __publicField
} from "./chunk-M4H726CL.js";

// node_modules/intl-tel-input/dist/js/intlTelInput.mjs
var rawCountryData = [
  [
    "af",
    // Afghanistan
    "93",
    0,
    null,
    "0"
  ],
  [
    "ax",
    // Åland Islands (AKA Aland Islands)
    "358",
    1,
    ["18", "4"],
    // (4 is a mobile range shared with FI)
    // NOTE: https://en.wikipedia.org/wiki/Telephone%20numbers%20in%20%C3%85land says some 4XXX ranges (e.g. 4570) are specific to AX, but LPN doesn't respect this (https://libphonenumber.appspot.com/phonenumberparser?number=%2B3584570123456 says region=FI) so we won't either. Also it's too much of a maintenance burden to keep track of. Keep the 4 area code range here so that if the user selects AX and types this kind of number, we wont change the flag to FI. Whereas if they type a FI-only range then we will.
    "0"
  ],
  [
    "al",
    // Albania
    "355",
    0,
    null,
    "0"
  ],
  [
    "dz",
    // Algeria
    "213",
    0,
    null,
    "0"
  ],
  [
    "as",
    // American Samoa
    "1",
    5,
    ["684"],
    "1"
  ],
  [
    "ad",
    // Andorra
    "376"
  ],
  [
    "ao",
    // Angola
    "244"
  ],
  [
    "ai",
    // Anguilla
    "1",
    6,
    ["264"],
    "1"
  ],
  [
    "ag",
    // Antigua and Barbuda
    "1",
    7,
    ["268"],
    "1"
  ],
  [
    "ar",
    // Argentina
    "54",
    0,
    null,
    "0"
  ],
  [
    "am",
    // Armenia
    "374",
    0,
    null,
    "0"
  ],
  [
    "aw",
    // Aruba
    "297"
  ],
  [
    "ac",
    // Ascension Island
    "247"
  ],
  [
    "au",
    // Australia
    "61",
    0,
    ["4"],
    // (mobile range shared with CX and CC)
    "0"
  ],
  [
    "at",
    // Austria
    "43",
    0,
    null,
    "0"
  ],
  [
    "az",
    // Azerbaijan
    "994",
    0,
    null,
    "0"
  ],
  [
    "bs",
    // Bahamas
    "1",
    8,
    ["242"],
    "1"
  ],
  [
    "bh",
    // Bahrain
    "973"
  ],
  [
    "bd",
    // Bangladesh
    "880",
    0,
    null,
    "0"
  ],
  [
    "bb",
    // Barbados
    "1",
    9,
    ["246"],
    "1"
  ],
  [
    "by",
    // Belarus
    "375",
    0,
    null,
    "8"
  ],
  [
    "be",
    // Belgium
    "32",
    0,
    null,
    "0"
  ],
  [
    "bz",
    // Belize
    "501"
  ],
  [
    "bj",
    // Benin
    "229"
  ],
  [
    "bm",
    // Bermuda
    "1",
    10,
    ["441"],
    "1"
  ],
  [
    "bt",
    // Bhutan
    "975"
  ],
  [
    "bo",
    // Bolivia
    "591",
    0,
    null,
    "0"
  ],
  [
    "ba",
    // Bosnia and Herzegovina
    "387",
    0,
    null,
    "0"
  ],
  [
    "bw",
    // Botswana
    "267"
  ],
  [
    "br",
    // Brazil
    "55",
    0,
    null,
    "0"
  ],
  [
    "io",
    // British Indian Ocean Territory
    "246"
  ],
  [
    "vg",
    // British Virgin Islands
    "1",
    11,
    ["284"],
    "1"
  ],
  [
    "bn",
    // Brunei
    "673"
  ],
  [
    "bg",
    // Bulgaria
    "359",
    0,
    null,
    "0"
  ],
  [
    "bf",
    // Burkina Faso
    "226"
  ],
  [
    "bi",
    // Burundi
    "257"
  ],
  [
    "kh",
    // Cambodia
    "855",
    0,
    null,
    "0"
  ],
  [
    "cm",
    // Cameroon
    "237"
  ],
  [
    "ca",
    // Canada
    "1",
    1,
    [
      "204",
      "226",
      "236",
      "249",
      "250",
      "257",
      "263",
      "289",
      "306",
      "343",
      "354",
      "365",
      "367",
      "368",
      "382",
      "403",
      "416",
      "418",
      "428",
      "431",
      "437",
      "438",
      "450",
      "468",
      "474",
      "506",
      "514",
      "519",
      "548",
      "579",
      "581",
      "584",
      "587",
      "604",
      "613",
      "639",
      "647",
      "672",
      "683",
      "705",
      "709",
      "742",
      "753",
      "778",
      "780",
      "782",
      "807",
      "819",
      "825",
      "867",
      "873",
      "879",
      "902",
      "905",
      "942"
    ],
    "1"
  ],
  [
    "cv",
    // Cape Verde
    "238"
  ],
  [
    "bq",
    // Caribbean Netherlands
    "599",
    1,
    ["3", "4", "7"]
  ],
  [
    "ky",
    // Cayman Islands
    "1",
    12,
    ["345"],
    "1"
  ],
  [
    "cf",
    // Central African Republic
    "236"
  ],
  [
    "td",
    // Chad
    "235"
  ],
  [
    "cl",
    // Chile
    "56"
  ],
  [
    "cn",
    // China
    "86",
    0,
    null,
    "0"
  ],
  [
    "cx",
    // Christmas Island
    "61",
    2,
    ["4", "89164"],
    // (4 is a mobile range shared with AU and CC)
    "0"
  ],
  [
    "cc",
    // Cocos (Keeling) Islands
    "61",
    1,
    ["4", "89162"],
    // (4 is a mobile range shared with AU and CX)
    "0"
  ],
  [
    "co",
    // Colombia
    "57",
    0,
    null,
    "0"
  ],
  [
    "km",
    // Comoros
    "269"
  ],
  [
    "cg",
    // Congo (Brazzaville)
    "242"
  ],
  [
    "cd",
    // Congo (Kinshasa)
    "243",
    0,
    null,
    "0"
  ],
  [
    "ck",
    // Cook Islands
    "682"
  ],
  [
    "cr",
    // Costa Rica
    "506"
  ],
  [
    "ci",
    // Côte d'Ivoire
    "225"
  ],
  [
    "hr",
    // Croatia
    "385",
    0,
    null,
    "0"
  ],
  [
    "cu",
    // Cuba
    "53",
    0,
    null,
    "0"
  ],
  [
    "cw",
    // Curaçao
    "599",
    0
  ],
  [
    "cy",
    // Cyprus
    "357"
  ],
  [
    "cz",
    // Czech Republic
    "420"
  ],
  [
    "dk",
    // Denmark
    "45"
  ],
  [
    "dj",
    // Djibouti
    "253"
  ],
  [
    "dm",
    // Dominica
    "1",
    13,
    ["767"],
    "1"
  ],
  [
    "do",
    // Dominican Republic
    "1",
    2,
    ["809", "829", "849"],
    "1"
  ],
  [
    "ec",
    // Ecuador
    "593",
    0,
    null,
    "0"
  ],
  [
    "eg",
    // Egypt
    "20",
    0,
    null,
    "0"
  ],
  [
    "sv",
    // El Salvador
    "503"
  ],
  [
    "gq",
    // Equatorial Guinea
    "240"
  ],
  [
    "er",
    // Eritrea
    "291",
    0,
    null,
    "0"
  ],
  [
    "ee",
    // Estonia
    "372"
  ],
  [
    "sz",
    // Eswatini
    "268"
  ],
  [
    "et",
    // Ethiopia
    "251",
    0,
    null,
    "0"
  ],
  [
    "fk",
    // Falkland Islands (Malvinas)
    "500"
  ],
  [
    "fo",
    // Faroe Islands
    "298"
  ],
  [
    "fj",
    // Fiji
    "679"
  ],
  [
    "fi",
    // Finland
    "358",
    0,
    ["4"],
    // (mobile range shared with AX)
    "0"
  ],
  [
    "fr",
    // France
    "33",
    0,
    null,
    "0"
  ],
  [
    "gf",
    // French Guiana
    "594",
    0,
    null,
    "0"
  ],
  [
    "pf",
    // French Polynesia
    "689"
  ],
  [
    "ga",
    // Gabon
    "241"
  ],
  [
    "gm",
    // Gambia
    "220"
  ],
  [
    "ge",
    // Georgia
    "995",
    0,
    null,
    "0"
  ],
  [
    "de",
    // Germany
    "49",
    0,
    null,
    "0"
  ],
  [
    "gh",
    // Ghana
    "233",
    0,
    null,
    "0"
  ],
  [
    "gi",
    // Gibraltar
    "350"
  ],
  [
    "gr",
    // Greece
    "30"
  ],
  [
    "gl",
    // Greenland
    "299"
  ],
  [
    "gd",
    // Grenada
    "1",
    14,
    ["473"],
    "1"
  ],
  [
    "gp",
    // Guadeloupe
    "590",
    0,
    null,
    "0"
  ],
  [
    "gu",
    // Guam
    "1",
    15,
    ["671"],
    "1"
  ],
  [
    "gt",
    // Guatemala
    "502"
  ],
  [
    "gg",
    // Guernsey
    "44",
    1,
    ["1481", "7781", "7839", "7911"],
    "0"
  ],
  [
    "gn",
    // Guinea
    "224"
  ],
  [
    "gw",
    // Guinea-Bissau
    "245"
  ],
  [
    "gy",
    // Guyana
    "592"
  ],
  [
    "ht",
    // Haiti
    "509"
  ],
  [
    "hn",
    // Honduras
    "504"
  ],
  [
    "hk",
    // Hong Kong SAR China
    "852"
  ],
  [
    "hu",
    // Hungary
    "36",
    0,
    null,
    "06"
  ],
  [
    "is",
    // Iceland
    "354"
  ],
  [
    "in",
    // India
    "91",
    0,
    null,
    "0"
  ],
  [
    "id",
    // Indonesia
    "62",
    0,
    null,
    "0"
  ],
  [
    "ir",
    // Iran
    "98",
    0,
    null,
    "0"
  ],
  [
    "iq",
    // Iraq
    "964",
    0,
    null,
    "0"
  ],
  [
    "ie",
    // Ireland
    "353",
    0,
    null,
    "0"
  ],
  [
    "im",
    // Isle of Man
    "44",
    2,
    ["1624", "74576", "7524", "7624", "7924"],
    "0"
  ],
  [
    "il",
    // Israel
    "972",
    0,
    null,
    "0"
  ],
  [
    "it",
    // Italy
    "39",
    0,
    ["3"]
    // (mobile range shared with VA)
  ],
  [
    "jm",
    // Jamaica
    "1",
    4,
    ["658", "876"],
    "1"
  ],
  [
    "jp",
    // Japan
    "81",
    0,
    null,
    "0"
  ],
  [
    "je",
    // Jersey
    "44",
    3,
    ["1534", "7509", "7700", "7797", "7829", "7937"],
    "0"
  ],
  [
    "jo",
    // Jordan
    "962",
    0,
    null,
    "0"
  ],
  [
    "kz",
    // Kazakhstan
    "7",
    1,
    ["33", "7"],
    // (33 is shared with RU)
    "8"
  ],
  [
    "ke",
    // Kenya
    "254",
    0,
    null,
    "0"
  ],
  [
    "ki",
    // Kiribati
    "686",
    0,
    null,
    "0"
  ],
  [
    "xk",
    // Kosovo
    "383",
    0,
    null,
    "0"
  ],
  [
    "kw",
    // Kuwait
    "965"
  ],
  [
    "kg",
    // Kyrgyzstan
    "996",
    0,
    null,
    "0"
  ],
  [
    "la",
    // Laos
    "856",
    0,
    null,
    "0"
  ],
  [
    "lv",
    // Latvia
    "371"
  ],
  [
    "lb",
    // Lebanon
    "961",
    0,
    null,
    "0"
  ],
  [
    "ls",
    // Lesotho
    "266"
  ],
  [
    "lr",
    // Liberia
    "231",
    0,
    null,
    "0"
  ],
  [
    "ly",
    // Libya
    "218",
    0,
    null,
    "0"
  ],
  [
    "li",
    // Liechtenstein
    "423",
    0,
    null,
    "0"
  ],
  [
    "lt",
    // Lithuania
    "370",
    0,
    null,
    "0"
  ],
  [
    "lu",
    // Luxembourg
    "352"
  ],
  [
    "mo",
    // Macao SAR China
    "853"
  ],
  [
    "mg",
    // Madagascar
    "261",
    0,
    null,
    "0"
  ],
  [
    "mw",
    // Malawi
    "265",
    0,
    null,
    "0"
  ],
  [
    "my",
    // Malaysia
    "60",
    0,
    null,
    "0"
  ],
  [
    "mv",
    // Maldives
    "960"
  ],
  [
    "ml",
    // Mali
    "223"
  ],
  [
    "mt",
    // Malta
    "356"
  ],
  [
    "mh",
    // Marshall Islands
    "692",
    0,
    null,
    "1"
  ],
  [
    "mq",
    // Martinique
    "596",
    0,
    null,
    "0"
  ],
  [
    "mr",
    // Mauritania
    "222"
  ],
  [
    "mu",
    // Mauritius
    "230"
  ],
  [
    "yt",
    // Mayotte
    "262",
    1,
    ["269", "639"],
    "0"
  ],
  [
    "mx",
    // Mexico
    "52"
  ],
  [
    "fm",
    // Micronesia
    "691"
  ],
  [
    "md",
    // Moldova
    "373",
    0,
    null,
    "0"
  ],
  [
    "mc",
    // Monaco
    "377",
    0,
    null,
    "0"
  ],
  [
    "mn",
    // Mongolia
    "976",
    0,
    null,
    "0"
  ],
  [
    "me",
    // Montenegro
    "382",
    0,
    null,
    "0"
  ],
  [
    "ms",
    // Montserrat
    "1",
    16,
    ["664"],
    "1"
  ],
  [
    "ma",
    // Morocco
    "212",
    0,
    ["6", "7"],
    // (mobile ranges shared with EH)
    "0"
  ],
  [
    "mz",
    // Mozambique
    "258"
  ],
  [
    "mm",
    // Myanmar (Burma)
    "95",
    0,
    null,
    "0"
  ],
  [
    "na",
    // Namibia
    "264",
    0,
    null,
    "0"
  ],
  [
    "nr",
    // Nauru
    "674"
  ],
  [
    "np",
    // Nepal
    "977",
    0,
    null,
    "0"
  ],
  [
    "nl",
    // Netherlands
    "31",
    0,
    null,
    "0"
  ],
  [
    "nc",
    // New Caledonia
    "687"
  ],
  [
    "nz",
    // New Zealand
    "64",
    0,
    null,
    "0"
  ],
  [
    "ni",
    // Nicaragua
    "505"
  ],
  [
    "ne",
    // Niger
    "227"
  ],
  [
    "ng",
    // Nigeria
    "234",
    0,
    null,
    "0"
  ],
  [
    "nu",
    // Niue
    "683"
  ],
  [
    "nf",
    // Norfolk Island
    "672"
  ],
  [
    "kp",
    // North Korea
    "850",
    0,
    null,
    "0"
  ],
  [
    "mk",
    // North Macedonia
    "389",
    0,
    null,
    "0"
  ],
  [
    "mp",
    // Northern Mariana Islands
    "1",
    17,
    ["670"],
    "1"
  ],
  [
    "no",
    // Norway
    "47",
    0,
    ["4", "9"]
    // (mobile ranges shared with SJ)
  ],
  [
    "om",
    // Oman
    "968"
  ],
  [
    "pk",
    // Pakistan
    "92",
    0,
    null,
    "0"
  ],
  [
    "pw",
    // Palau
    "680"
  ],
  [
    "ps",
    // Palestinian Territories
    "970",
    0,
    null,
    "0"
  ],
  [
    "pa",
    // Panama
    "507"
  ],
  [
    "pg",
    // Papua New Guinea
    "675"
  ],
  [
    "py",
    // Paraguay
    "595",
    0,
    null,
    "0"
  ],
  [
    "pe",
    // Peru
    "51",
    0,
    null,
    "0"
  ],
  [
    "ph",
    // Philippines
    "63",
    0,
    null,
    "0"
  ],
  [
    "pl",
    // Poland
    "48"
  ],
  [
    "pt",
    // Portugal
    "351"
  ],
  [
    "pr",
    // Puerto Rico
    "1",
    3,
    ["787", "939"],
    "1"
  ],
  [
    "qa",
    // Qatar
    "974"
  ],
  [
    "re",
    // Réunion
    "262",
    0,
    null,
    "0"
  ],
  [
    "ro",
    // Romania
    "40",
    0,
    null,
    "0"
  ],
  [
    "ru",
    // Russia
    "7",
    0,
    ["33"],
    // (shared with KZ)
    "8"
  ],
  [
    "rw",
    // Rwanda
    "250",
    0,
    null,
    "0"
  ],
  [
    "ws",
    // Samoa
    "685"
  ],
  [
    "sm",
    // San Marino
    "378"
  ],
  [
    "st",
    // São Tomé & Príncipe
    "239"
  ],
  [
    "sa",
    // Saudi Arabia
    "966",
    0,
    null,
    "0"
  ],
  [
    "sn",
    // Senegal
    "221"
  ],
  [
    "rs",
    // Serbia
    "381",
    0,
    null,
    "0"
  ],
  [
    "sc",
    // Seychelles
    "248"
  ],
  [
    "sl",
    // Sierra Leone
    "232",
    0,
    null,
    "0"
  ],
  [
    "sg",
    // Singapore
    "65"
  ],
  [
    "sx",
    // Sint Maarten
    "1",
    21,
    ["721"],
    "1"
  ],
  [
    "sk",
    // Slovakia
    "421",
    0,
    null,
    "0"
  ],
  [
    "si",
    // Slovenia
    "386",
    0,
    null,
    "0"
  ],
  [
    "sb",
    // Solomon Islands
    "677"
  ],
  [
    "so",
    // Somalia
    "252",
    0,
    null,
    "0"
  ],
  [
    "za",
    // South Africa
    "27",
    0,
    null,
    "0"
  ],
  [
    "kr",
    // South Korea
    "82",
    0,
    null,
    "0"
  ],
  [
    "ss",
    // South Sudan
    "211",
    0,
    null,
    "0"
  ],
  [
    "es",
    // Spain
    "34"
  ],
  [
    "lk",
    // Sri Lanka
    "94",
    0,
    null,
    "0"
  ],
  [
    "bl",
    // St. Barthélemy
    "590",
    1,
    null,
    "0"
  ],
  [
    "sh",
    // St. Helena
    "290"
  ],
  [
    "kn",
    // St. Kitts & Nevis
    "1",
    18,
    ["869"],
    "1"
  ],
  [
    "lc",
    // St. Lucia
    "1",
    19,
    ["758"],
    "1"
  ],
  [
    "mf",
    // St. Martin
    "590",
    2,
    null,
    "0"
  ],
  [
    "pm",
    // St. Pierre & Miquelon
    "508",
    0,
    null,
    "0"
  ],
  [
    "vc",
    // St. Vincent & Grenadines
    "1",
    20,
    ["784"],
    "1"
  ],
  [
    "sd",
    // Sudan
    "249",
    0,
    null,
    "0"
  ],
  [
    "sr",
    // Suriname
    "597"
  ],
  [
    "sj",
    // Svalbard & Jan Mayen
    "47",
    1,
    ["4", "79", "9"]
    // (4 and 9 are mobile ranges shared with NO)
  ],
  [
    "se",
    // Sweden
    "46",
    0,
    null,
    "0"
  ],
  [
    "ch",
    // Switzerland
    "41",
    0,
    null,
    "0"
  ],
  [
    "sy",
    // Syria
    "963",
    0,
    null,
    "0"
  ],
  [
    "tw",
    // Taiwan
    "886",
    0,
    null,
    "0"
  ],
  [
    "tj",
    // Tajikistan
    "992"
  ],
  [
    "tz",
    // Tanzania
    "255",
    0,
    null,
    "0"
  ],
  [
    "th",
    // Thailand
    "66",
    0,
    null,
    "0"
  ],
  [
    "tl",
    // Timor-Leste
    "670"
  ],
  [
    "tg",
    // Togo
    "228"
  ],
  [
    "tk",
    // Tokelau
    "690"
  ],
  [
    "to",
    // Tonga
    "676"
  ],
  [
    "tt",
    // Trinidad & Tobago
    "1",
    22,
    ["868"],
    "1"
  ],
  [
    "tn",
    // Tunisia
    "216"
  ],
  [
    "tr",
    // Turkey
    "90",
    0,
    null,
    "0"
  ],
  [
    "tm",
    // Turkmenistan
    "993",
    0,
    null,
    "8"
  ],
  [
    "tc",
    // Turks & Caicos Islands
    "1",
    23,
    ["649"],
    "1"
  ],
  [
    "tv",
    // Tuvalu
    "688"
  ],
  [
    "vi",
    // U.S. Virgin Islands
    "1",
    24,
    ["340"],
    "1"
  ],
  [
    "ug",
    // Uganda
    "256",
    0,
    null,
    "0"
  ],
  [
    "ua",
    // Ukraine
    "380",
    0,
    null,
    "0"
  ],
  [
    "ae",
    // United Arab Emirates
    "971",
    0,
    null,
    "0"
  ],
  [
    "gb",
    // United Kingdom
    "44",
    0,
    null,
    "0"
  ],
  [
    "us",
    // United States
    "1",
    0,
    null,
    "1"
  ],
  [
    "uy",
    // Uruguay
    "598",
    0,
    null,
    "0"
  ],
  [
    "uz",
    // Uzbekistan
    "998"
  ],
  [
    "vu",
    // Vanuatu
    "678"
  ],
  [
    "va",
    // Vatican City
    "39",
    1,
    ["06698", "3"]
    // (3 is a mobile range shared with IT)
  ],
  [
    "ve",
    // Venezuela
    "58",
    0,
    null,
    "0"
  ],
  [
    "vn",
    // Vietnam
    "84",
    0,
    null,
    "0"
  ],
  [
    "wf",
    // Wallis & Futuna
    "681"
  ],
  [
    "eh",
    // Western Sahara
    "212",
    1,
    ["5288", "5289", "6", "7"],
    // (6 and 7 are mobile ranges shared with MA)
    "0"
  ],
  [
    "ye",
    // Yemen
    "967",
    0,
    null,
    "0"
  ],
  [
    "zm",
    // Zambia
    "260",
    0,
    null,
    "0"
  ],
  [
    "zw",
    // Zimbabwe
    "263",
    0,
    null,
    "0"
  ]
];
var allCountries = [];
for (const c of rawCountryData) {
  allCountries.push({
    name: "",
    // populated in the plugin
    iso2: c[0],
    dialCode: c[1],
    priority: c[2] || 0,
    areaCodes: c[3] || null,
    nationalPrefix: c[4] || null,
    normalisedName: "",
    // populated in the plugin
    initials: "",
    // populated in the plugin
    dialCodePlus: ""
    // populated in the plugin
  });
}
var iso2Set = new Set(allCountries.map((c) => c.iso2));
var isIso2 = (val) => iso2Set.has(val);
var data_default = allCountries;
var EVENTS = {
  OPEN_COUNTRY_DROPDOWN: "open:countrydropdown",
  CLOSE_COUNTRY_DROPDOWN: "close:countrydropdown",
  COUNTRY_CHANGE: "countrychange",
  INPUT: "input",
  // used for synthetic input trigger
  STRICT_REJECT: "strict:reject"
};
var CLASSES = {
  HIDE: "iti__hide",
  V_HIDE: "iti__v-hide",
  ARROW_UP: "iti__arrow--up",
  GLOBE: "iti__globe",
  FLAG: "iti__flag",
  LOADING: "iti__loading",
  COUNTRY_ITEM: "iti__country",
  HIGHLIGHT: "iti__highlight"
};
var KEYS = {
  ARROW_UP: "ArrowUp",
  ARROW_DOWN: "ArrowDown",
  SPACE: " ",
  ENTER: "Enter",
  ESC: "Escape",
  TAB: "Tab"
};
var INPUT_TYPES = {
  PASTE: "insertFromPaste",
  DELETE_FORWARD: "deleteContentForward"
};
var REGEX = {
  ALPHA_UNICODE: new RegExp("\\p{L}", "u"),
  // any kind of letter from any language
  NON_PLUS_NUMERIC: /[^+0-9]/,
  // chars that are NOT + or digit
  NON_PLUS_NUMERIC_GLOBAL: /[^+0-9]/g,
  // chars that are NOT + or digit (global)
  HIDDEN_SEARCH_CHAR: /^[a-zA-ZÀ-ÿа-яА-Я ]$/
  // single acceptable hidden-search char
};
var TIMINGS = {
  SEARCH_DEBOUNCE_MS: 100,
  HIDDEN_SEARCH_RESET_MS: 1e3,
  NEXT_TICK: 0
};
var SENTINELS = {
  UNKNOWN_NUMBER_TYPE: -99,
  UNKNOWN_VALIDATION_ERROR: -99
};
var LAYOUT = {
  NARROW_VIEWPORT_WIDTH: 500,
  // keep in sync with .iti__country-list CSS media query
  FALLBACK_SELECTED_WITH_DIAL_WIDTH: 78,
  // px width fallback when separateDialCode enabled
  FALLBACK_SELECTED_NO_DIAL_WIDTH: 42,
  // px width fallback when no separate dial code
  INPUT_PADDING_EXTRA_LEFT: 6,
  // px gap between selected country container and input text
  DROPDOWN_MARGIN: 3,
  // px margin between dropdown and tel input
  FALLBACK_DROPDOWN_HEIGHT: 200
  // px height fallback for dropdown
};
var DIAL_CODE = {
  PLUS: "+",
  NANP: "1"
  // North American Numbering Plan
};
var UK = {
  ISO2: "gb",
  DIAL_CODE: "44",
  // +44 United Kingdom
  MOBILE_PREFIX: "7",
  // UK mobile numbers start with 7 after national trunk (0) or core section
  MOBILE_CORE_LENGTH: 10
  // core number length (excluding dial code / national prefix) for mobiles
};
var US = {
  ISO2: "us",
  DIAL_CODE: "1"
  // +1 United States
};
var PLACEHOLDER_MODES = {
  AGGRESSIVE: "aggressive",
  POLITE: "polite",
  OFF: "off"
};
var INITIAL_COUNTRY = {
  AUTO: "auto"
};
var NUMBER_TYPES = [
  "FIXED_LINE",
  "MOBILE",
  "FIXED_LINE_OR_MOBILE",
  "TOLL_FREE",
  "PREMIUM_RATE",
  "SHARED_COST",
  "VOIP",
  "PERSONAL_NUMBER",
  "PAGER",
  "UAN",
  "VOICEMAIL",
  "UNKNOWN"
];
var NUMBER_TYPE_SET = new Set(NUMBER_TYPES);
var DATA_KEYS = {
  // e.g. <li data-country-code="us"> for country items in dropdown
  // in the future, the value should be changed to "iso2", but this is a breaking change for another day
  ISO2: "countryCode",
  DIAL_CODE: "dialCode",
  // e.g. <input data-intl-tel-input-id="0"> on the input element
  INSTANCE_ID: "intlTelInputId"
};
var ARIA = {
  EXPANDED: "aria-expanded",
  LABEL: "aria-label",
  SELECTED: "aria-selected",
  ACTIVE_DESCENDANT: "aria-activedescendant",
  HASPOPUP: "aria-haspopup",
  CONTROLS: "aria-controls",
  HIDDEN: "aria-hidden",
  AUTOCOMPLETE: "aria-autocomplete",
  MODAL: "aria-modal"
};
var interfaceTranslations = {
  selectedCountryAriaLabel: "Change country for phone number, currently selected ${countryName} (${dialCode})",
  noCountrySelected: "Select country for phone number",
  countryListAriaLabel: "List of countries",
  searchPlaceholder: "Search",
  clearSearchAriaLabel: "Clear search",
  searchEmptyState: "No results found",
  searchSummaryAria(count) {
    if (count === 0) {
      return "No results found";
    }
    if (count === 1) {
      return "1 result found";
    }
    return `${count} results found`;
  }
};
var en_default = interfaceTranslations;
var mediaQuery = (q) => typeof window !== "undefined" && typeof window.matchMedia === "function" && window.matchMedia(q).matches;
var isNarrowViewport = () => mediaQuery(`(max-width: ${LAYOUT.NARROW_VIEWPORT_WIDTH}px)`);
var computeDefaultUseFullscreenPopup = () => {
  if (typeof navigator !== "undefined" && typeof window !== "undefined") {
    const isShortViewport = mediaQuery("(max-height: 600px)");
    const isCoarsePointer = mediaQuery("(pointer: coarse)");
    return isNarrowViewport() || isCoarsePointer && isShortViewport;
  }
  return false;
};
var defaults = {
  //* Whether or not to allow the dropdown.
  allowDropdown: true,
  //* The number type to enforce during validation.
  allowedNumberTypes: ["MOBILE", "FIXED_LINE"],
  //* Whether or not to allow extensions after the main number.
  allowNumberExtensions: false,
  // Allow alphanumeric "phonewords" (e.g. +1 800 FLOWERS) as valid numbers
  allowPhonewords: false,
  //* Add a placeholder in the input with an example number for the selected country.
  autoPlaceholder: PLACEHOLDER_MODES.POLITE,
  //* Add a custom class to the (injected) container element.
  containerClass: "",
  //* Locale for localising country names via Intl.DisplayNames.
  countryNameLocale: "en",
  //* The order of the countries in the dropdown. Defaults to alphabetical.
  countryOrder: null,
  //* Add a country search input at the top of the dropdown.
  countrySearch: true,
  //* Modify the auto placeholder.
  customPlaceholder: null,
  //* Always show the dropdown
  dropdownAlwaysOpen: false,
  //* Append menu to specified element.
  dropdownContainer: null,
  //* Don't display these countries.
  excludeCountries: null,
  //* Fix the dropdown width to the input width (rather than being as wide as the longest country name).
  fixDropdownWidth: true,
  //* Format the number as the user types
  formatAsYouType: true,
  //* Format the input value during initialisation and on setNumber.
  formatOnDisplay: true,
  //* geoIp lookup function.
  geoIpLookup: null,
  //* Inject a hidden input with the name returned from this function, and on submit, populate it with the result of getNumber.
  hiddenInput: null,
  //* Internationalise the plugin text e.g. search input placeholder, country names.
  i18n: {},
  //* Initial country.
  initialCountry: "",
  //* A function to load the utils script.
  loadUtils: null,
  //* National vs international formatting for numbers e.g. placeholders and displaying existing numbers.
  nationalMode: true,
  //* Display only these countries.
  onlyCountries: null,
  //* Number type to use for placeholders.
  placeholderNumberType: "MOBILE",
  //* Add custom classes to the search input element.
  searchInputClass: "",
  //* Display the international dial code next to the selected flag.
  separateDialCode: false,
  //* Show flags - for both the selected country, and in the country dropdown
  showFlags: true,
  //* Only allow certain chars e.g. a plus followed by numeric digits, and cap at max valid length.
  strictMode: false,
  //* Use full screen popup instead of dropdown for country list.
  useFullscreenPopup: computeDefaultUseFullscreenPopup()
};
var toString = (val) => JSON.stringify(val);
var isPlainObject = (val) => Boolean(val) && typeof val === "object" && !Array.isArray(val);
var isFunction = (val) => typeof val === "function";
var isElLike = (val) => {
  if (!val || typeof val !== "object") {
    return false;
  }
  const v = val;
  return v.nodeType === 1 && typeof v.tagName === "string" && typeof v.appendChild === "function";
};
var placeholderModeSet = new Set(Object.values(PLACEHOLDER_MODES));
var warn = (message) => {
  console.warn(`[intl-tel-input] ${message}`);
};
var warnOption = (optionName, expectedType, actualValue) => {
  warn(
    `Option '${optionName}' must be ${expectedType}; got ${toString(actualValue)}. Ignoring.`
  );
};
var validateIso2Array = (key, value) => {
  const expectedType = "an array of iso2 country code strings";
  if (!Array.isArray(value)) {
    warnOption(key, expectedType, value);
    return false;
  }
  const valid = [];
  for (const v of value) {
    if (typeof v !== "string") {
      warnOption(key, expectedType, value);
      return false;
    }
    const lower = v.toLowerCase();
    if (!isIso2(lower)) {
      warn(`Invalid iso2 code in '${key}': '${v}'. Skipping.`);
    } else {
      valid.push(v);
    }
  }
  return valid;
};
var validateOptions = (customOptions) => {
  if (customOptions === void 0) {
    return {};
  }
  if (!isPlainObject(customOptions)) {
    const error = `The second argument must be an options object; got ${toString(customOptions)}. Using defaults.`;
    warn(error);
    return {};
  }
  const validatedOptions = {};
  for (const [key, value] of Object.entries(customOptions)) {
    if (!Object.hasOwn(defaults, key)) {
      warn(`Unknown option '${key}'. Ignoring.`);
      continue;
    }
    switch (key) {
      case "allowDropdown":
      case "allowNumberExtensions":
      case "allowPhonewords":
      case "countrySearch":
      case "dropdownAlwaysOpen":
      case "fixDropdownWidth":
      case "formatAsYouType":
      case "formatOnDisplay":
      case "nationalMode":
      case "showFlags":
      case "separateDialCode":
      case "strictMode":
      case "useFullscreenPopup":
        if (typeof value !== "boolean") {
          warnOption(key, "a boolean", value);
          break;
        }
        validatedOptions[key] = value;
        break;
      case "autoPlaceholder":
        if (typeof value !== "string" || !placeholderModeSet.has(value)) {
          const validModes = Array.from(placeholderModeSet).join(", ");
          warnOption("autoPlaceholder", `one of ${validModes}`, value);
          break;
        }
        validatedOptions[key] = value;
        break;
      case "containerClass":
      case "searchInputClass":
      case "countryNameLocale":
        if (typeof value !== "string") {
          warnOption(key, "a string", value);
          break;
        }
        validatedOptions[key] = value;
        break;
      case "countryOrder": {
        if (value === null) {
          validatedOptions[key] = value;
        } else {
          const filtered = validateIso2Array(key, value);
          if (filtered !== false) {
            validatedOptions[key] = filtered;
          }
        }
        break;
      }
      case "customPlaceholder":
      case "geoIpLookup":
      case "hiddenInput":
      case "loadUtils":
        if (value !== null && !isFunction(value)) {
          warnOption(key, "a function or null", value);
          break;
        }
        validatedOptions[key] = value;
        break;
      case "dropdownContainer":
        if (value !== null && !isElLike(value)) {
          warnOption("dropdownContainer", "an HTMLElement or null", value);
          break;
        }
        validatedOptions[key] = value;
        break;
      case "excludeCountries":
      case "onlyCountries": {
        if (value === null) {
          validatedOptions[key] = value;
        } else {
          const filtered = validateIso2Array(key, value);
          if (filtered !== false) {
            validatedOptions[key] = filtered;
          }
        }
        break;
      }
      case "i18n":
        if (value && !isPlainObject(value)) {
          warnOption("i18n", "an object", value);
          break;
        }
        validatedOptions[key] = value;
        break;
      case "initialCountry": {
        if (typeof value !== "string") {
          warnOption("initialCountry", "a string", value);
          break;
        }
        const lower = value.toLowerCase();
        if (lower && lower !== INITIAL_COUNTRY.AUTO && !isIso2(lower)) {
          warnOption(
            "initialCountry",
            "a valid iso2 country code or 'auto'",
            value
          );
          break;
        }
        validatedOptions[key] = value;
        break;
      }
      case "placeholderNumberType":
        if (typeof value !== "string" || !NUMBER_TYPE_SET.has(value)) {
          const validTypes = Array.from(NUMBER_TYPE_SET).join(", ");
          warnOption("placeholderNumberType", `one of ${validTypes}`, value);
          break;
        }
        validatedOptions[key] = value;
        break;
      case "allowedNumberTypes":
        if (value !== null) {
          if (!Array.isArray(value)) {
            warnOption(
              "allowedNumberTypes",
              "an array of number types or null",
              value
            );
            break;
          }
          let allValid = true;
          for (const v of value) {
            if (typeof v !== "string" || !NUMBER_TYPE_SET.has(v)) {
              const validTypes = Array.from(NUMBER_TYPE_SET).join(", ");
              warnOption(
                "allowedNumberTypes",
                `an array of valid number types (${validTypes})`,
                v
              );
              allValid = false;
              break;
            }
          }
          if (allValid) {
            validatedOptions[key] = value;
          }
        } else {
          validatedOptions[key] = null;
        }
        break;
    }
  }
  return validatedOptions;
};
var normaliseOptions = (o) => {
  var _a4, _b;
  if (o.initialCountry) {
    o.initialCountry = o.initialCountry.toLowerCase();
  }
  if ((_a4 = o.onlyCountries) == null ? void 0 : _a4.length) {
    o.onlyCountries = o.onlyCountries.map((c) => c.toLowerCase());
  }
  if ((_b = o.excludeCountries) == null ? void 0 : _b.length) {
    o.excludeCountries = o.excludeCountries.map((c) => c.toLowerCase());
  }
  if (o.countryOrder) {
    o.countryOrder = o.countryOrder.map((c) => c.toLowerCase());
  }
};
var applyOptionSideEffects = (o) => {
  var _a4;
  if (o.dropdownAlwaysOpen) {
    o.useFullscreenPopup = false;
    o.allowDropdown = true;
  }
  if (o.useFullscreenPopup) {
    o.fixDropdownWidth = false;
  } else {
    if (isNarrowViewport()) {
      o.fixDropdownWidth = true;
    }
  }
  if (((_a4 = o.onlyCountries) == null ? void 0 : _a4.length) === 1) {
    o.initialCountry = o.onlyCountries[0];
  }
  if (o.separateDialCode) {
    o.nationalMode = false;
  }
  if (o.allowDropdown && !o.showFlags && !o.separateDialCode) {
    o.nationalMode = false;
  }
  if (o.useFullscreenPopup && !o.dropdownContainer) {
    o.dropdownContainer = document.body;
  }
  o.i18n = { ...en_default, ...o.i18n };
};
var getNumeric = (s) => s.replace(/\D/g, "");
var normaliseString = (s = "") => s.normalize("NFD").replace(/[\u0300-\u036f]/g, "").toLowerCase();
var buildClassNames = (flags) => Object.keys(flags).filter((k) => Boolean(flags[k])).join(" ");
var createEl = (tagName, attrs, container) => {
  const el = document.createElement(tagName);
  if (attrs) {
    Object.entries(attrs).forEach(
      ([key, value]) => el.setAttribute(key, value)
    );
  }
  if (container) {
    container.appendChild(el);
  }
  return el;
};
var buildSearchIcon = () => `
  <svg class="iti__search-icon-svg" width="14" height="14" viewBox="0 0 24 24" focusable="false" ${ARIA.HIDDEN}="true">
    <circle cx="11" cy="11" r="7" />
    <line x1="21" y1="21" x2="16.65" y2="16.65" />
  </svg>`;
var buildClearIcon = (id) => {
  const maskId = `iti-${id}-clear-mask`;
  return `
    <svg class="iti__search-clear-svg" width="12" height="12" viewBox="0 0 16 16" ${ARIA.HIDDEN}="true" focusable="false">
      <mask id="${maskId}" maskUnits="userSpaceOnUse">
        <rect width="16" height="16" fill="white" />
        <path d="M5.2 5.2 L10.8 10.8 M10.8 5.2 L5.2 10.8" stroke="black" stroke-linecap="round" class="iti__search-clear-x" />
      </mask>
      <circle cx="8" cy="8" r="8" class="iti__search-clear-bg" mask="url(#${maskId})" />
    </svg>`;
};
var buildCheckIcon = () => `
  <svg class="iti__country-check-svg" width="14" height="14" viewBox="0 0 16 16" fill="currentColor" focusable="false" ${ARIA.HIDDEN}="true">
    <path d="M16 8A8 8 0 1 1 0 8a8 8 0 0 1 16 0m-3.97-3.03a.75.75 0 0 0-1.08.022L7.477 9.417 5.384 7.323a.75.75 0 0 0-1.06 1.06L6.97 11.03a.75.75 0 0 0 1.079-.02l3.992-4.99a.75.75 0 0 0-.01-1.05z"/>
  </svg>`;
var buildGlobeIcon = () => `
  <svg width="256" height="256" viewBox="0 0 512 512" class="iti__globe-svg">
    <path d="M508 213a240 240 0 0 0-449-87l-2 5-2 5c-8 14-13 30-17 46a65 65 0 0 1 56 4c16-10 35-19 56-27l9-3c-6 23-10 48-10 74h-16l4 6c3 4 5 8 6 13h6c0 22 3 44 8 65l2 10-25-10-4 5 12 18 9 3 6 2 8 3 9 26 1 2 16-7h1l-5-13-1-2c24 6 49 9 75 10v26l11 10 7 7v-30l1-13c22 0 44-3 65-8l10-2-21 48-1 1a317 317 0 0 1-14 23l-21 5h-2c6 16 7 33 1 50a240 240 0 0 0 211-265m-401-56-11 6c19-44 54-79 98-98-11 20-21 44-29 69-21 6-40 15-58 23m154 182v4c-29-1-57-6-81-13-7-25-12-52-13-81h94zm0-109h-94c1-29 6-56 13-81 24-7 52-12 81-13zm0-112c-22 1-44 4-65 8l-10 2 12-30 9-17 1-2a332 332 0 0 1 13-23c13-4 26-6 40-7zm187 69 6 4c4 12 6 25 6 38v1h-68c-1-26-4-51-10-74l48 20 1 1 14 8zm-14-44 10 20c-20-11-43-21-68-29-8-25-18-49-29-69 37 16 67 44 87 78M279 49h1c13 1 27 3 39 7l14 23 1 2a343 343 0 0 1 12 26l2 5 6 16c-23-6-48-9-74-10h-1zm0 87h1c29 1 56 6 81 13 7 24 12 51 12 80v1h-94zm2 207h-2v-94h95c-1 29-6 56-13 81-24 7-51 12-80 13m86 60-20 10c11-20 21-43 29-68 25-8 48-18 68-29-16 37-43 67-77 87m87-115-7 5-16 9-2 1a337 337 0 0 1-47 21c6-24 9-49 10-75h68c0 13-2 27-6 39"/>
    <path d="m261 428-2-2-22-21a40 40 0 0 0-32-11h-1a37 37 0 0 0-18 8l-1 1-4 2-2 2-5 4c-9-3-36-31-47-44s-32-45-34-55l3-2a151 151 0 0 0 11-9v-1a39 39 0 0 0 5-48l-3-3-11-19-3-4-5-7h-1l-3-3-4-3-5-2a35 35 0 0 0-16-3h-5c-4 1-14 5-24 11l-4 2-4 3-4 2c-9 8-17 17-18 27a380 380 0 0 0 212 259h3c12 0 25-10 36-21l10-12 6-11a39 39 0 0 0-8-40"/>
  </svg>`;
var getMatchedCountries = (countries, query) => {
  const normalisedQuery = normaliseString(query);
  const iso2Matches = [];
  const nameStartsWith = [];
  const nameContains = [];
  const dialCodeMatches = [];
  const dialCodeContains = [];
  const initialsMatches = [];
  for (const c of countries) {
    if (c.iso2 === normalisedQuery) {
      iso2Matches.push(c);
    } else if (c.normalisedName.startsWith(normalisedQuery)) {
      nameStartsWith.push(c);
    } else if (c.normalisedName.includes(normalisedQuery)) {
      nameContains.push(c);
    } else if (normalisedQuery === c.dialCode || normalisedQuery === c.dialCodePlus) {
      dialCodeMatches.push(c);
    } else if (c.dialCodePlus.includes(normalisedQuery)) {
      dialCodeContains.push(c);
    } else if (c.initials.includes(normalisedQuery)) {
      initialsMatches.push(c);
    }
  }
  const sortByPriority = (a, b) => a.priority - b.priority;
  return [
    ...iso2Matches,
    ...nameStartsWith,
    ...nameContains,
    // priority sort is only relevant when showing multiple countries with the same dial code (that's what the priority field is used to distinguish between)
    ...dialCodeMatches.sort(sortByPriority),
    ...dialCodeContains.sort(sortByPriority),
    ...initialsMatches
  ];
};
var findFirstCountryStartingWith = (countries, query) => {
  const normalisedQuery = normaliseString(query);
  for (const c of countries) {
    if (c.normalisedName.startsWith(normalisedQuery)) {
      return c;
    }
  }
  return null;
};
var _options, _id, _isRTL, _originalPaddingLeft, _countries, _searchDebounceTimer, _inlineDropdownHeight, _countryContainerEl, _selectedCountryEl, _selectedFlagEl, _selectedDialCodeEl, _dropdownArrowEl, _dropdownContentEl, _searchIconEl, _searchInputEl, _searchClearButtonEl, _countryListEl, _hiddenInputPhoneEl, _hiddenInputCountryEl, _noResultsMessageEl, _searchResultsLiveRegionEl, _detachedDropdownEl, _selectedListItemEl, _highlightedListItemEl, _listItemByIso2, _dropdownAbortController, __UI_instances, createWrapperAndInsert_fn, buildCountryContainer_fn, buildDropdownContent_fn, buildSearchUI_fn, updateInputPaddingAndReveal_fn, buildHiddenInputs_fn, appendListItems_fn, updateInputPadding_fn, __UI_static, _a, getBody_fn, getHiddenSelectedCountryWidth_fn, getHiddenInlineDropdownHeight_fn, updateSearchResultsA11yText_fn, filterCountriesByQuery_fn, applySearchFilter_fn, handleSearchChange_fn, handleSearchClear_fn, scrollCountryListToItem_fn, highlightListItem_fn, bindDropdownOpenListeners_fn, bindListItemHover_fn, bindListItemClick_fn, bindOutsideClickToClose_fn, bindDropdownKeydownListener_fn, bindSearchInputListener_fn, searchForCountry_fn, handleUpDownKey_fn, updateSelectedListItem_fn, showFilteredCountries_fn, shouldPositionInlineDropdownBelowInput_fn, injectAndPositionDetachedDropdown_fn, adjustFullscreenPopupToViewport_fn;
var UI = (_a = class {
  constructor(input, options, id) {
    __privateAdd(this, __UI_instances);
    // private
    __privateAdd(this, _options);
    __privateAdd(this, _id);
    __privateAdd(this, _isRTL);
    __privateAdd(this, _originalPaddingLeft, "");
    __privateAdd(this, _countries);
    __privateAdd(this, _searchDebounceTimer, null);
    __privateAdd(this, _inlineDropdownHeight);
    __privateAdd(this, _countryContainerEl);
    __privateAdd(this, _selectedCountryEl);
    __privateAdd(this, _selectedFlagEl);
    __privateAdd(this, _selectedDialCodeEl);
    __privateAdd(this, _dropdownArrowEl);
    __privateAdd(this, _dropdownContentEl);
    __privateAdd(this, _searchIconEl);
    __privateAdd(this, _searchInputEl);
    __privateAdd(this, _searchClearButtonEl);
    __privateAdd(this, _countryListEl);
    __privateAdd(this, _hiddenInputPhoneEl);
    __privateAdd(this, _hiddenInputCountryEl);
    __privateAdd(this, _noResultsMessageEl);
    __privateAdd(this, _searchResultsLiveRegionEl);
    __privateAdd(this, _detachedDropdownEl);
    __privateAdd(this, _selectedListItemEl, null);
    __privateAdd(this, _highlightedListItemEl, null);
    __privateAdd(this, _listItemByIso2, /* @__PURE__ */ new Map());
    __privateAdd(this, _dropdownAbortController, null);
    // public
    __publicField(this, "telInputEl");
    __publicField(this, "hadInitialPlaceholder");
    input.dataset[DATA_KEYS.INSTANCE_ID] = id.toString();
    this.telInputEl = input;
    __privateSet(this, _options, options);
    __privateSet(this, _id, id);
    this.hadInitialPlaceholder = Boolean(input.getAttribute("placeholder"));
    __privateSet(this, _isRTL, !!this.telInputEl.closest("[dir=rtl]"));
    __privateSet(this, _originalPaddingLeft, this.telInputEl.style.paddingLeft);
  }
  // Validate that the provided element is an HTMLInputElement.
  static validateInput(input) {
    const tagName = input == null ? void 0 : input.tagName;
    const isInputEl = Boolean(input) && typeof input === "object" && tagName === "INPUT" && typeof input.setAttribute === "function";
    if (!isInputEl) {
      const type = Object.prototype.toString.call(input);
      throw new TypeError(
        `The first argument must be an HTMLInputElement, not ${type}`
      );
    }
  }
  //* Generate all of the markup for the plugin: the selected country overlay, and the dropdown.
  buildMarkup(countries) {
    __privateSet(this, _countries, countries);
    this.telInputEl.classList.add("iti__tel-input");
    if (!this.telInputEl.hasAttribute("type")) {
      this.telInputEl.setAttribute("type", "tel");
    }
    if (!this.telInputEl.hasAttribute("autocomplete")) {
      this.telInputEl.setAttribute("autocomplete", "tel");
    }
    if (!this.telInputEl.hasAttribute("inputmode")) {
      this.telInputEl.setAttribute("inputmode", "tel");
    }
    const wrapper = __privateMethod(this, __UI_instances, createWrapperAndInsert_fn).call(this);
    __privateMethod(this, __UI_instances, buildCountryContainer_fn).call(this, wrapper);
    wrapper.appendChild(this.telInputEl);
    __privateMethod(this, __UI_instances, updateInputPaddingAndReveal_fn).call(this);
    __privateMethod(this, __UI_instances, buildHiddenInputs_fn).call(this, wrapper);
    this.ensureDropdownWidthSet();
  }
  ensureDropdownWidthSet() {
    const { fixDropdownWidth, allowDropdown } = __privateGet(this, _options);
    if (!allowDropdown || !fixDropdownWidth || __privateGet(this, _dropdownContentEl).style.width) {
      return;
    }
    const inputWidth = this.telInputEl.offsetWidth;
    if (inputWidth > 0) {
      __privateGet(this, _dropdownContentEl).style.width = `${inputWidth}px`;
    }
  }
  //* Pre-fill the search input with "+" and show all countries
  //* (used when user types "+" in the phone input to open the dropdown).
  //* Explicitly focus the search input (openDropdown skips this when
  //* dropdownAlwaysOpen, but here we need focus to redirect subsequent keystrokes).
  prefillSearchWithPlus() {
    __privateGet(this, _searchInputEl).value = "+";
    __privateGet(this, _searchInputEl).focus();
    __privateMethod(this, __UI_instances, filterCountriesByQuery_fn).call(this, "");
  }
  //* Bind a form-submit listener that syncs the hidden inputs with the current phone number
  //* and country iso2. No-op if there are no hidden inputs or the input is not in a form.
  bindHiddenInputSubmitListener(signal, getPhone, getCountryIso2) {
    const form = this.telInputEl.form;
    if (!form || !__privateGet(this, _hiddenInputPhoneEl) && !__privateGet(this, _hiddenInputCountryEl)) {
      return;
    }
    form.addEventListener(
      "submit",
      () => {
        if (__privateGet(this, _hiddenInputPhoneEl)) {
          __privateGet(this, _hiddenInputPhoneEl).value = getPhone();
        }
        if (__privateGet(this, _hiddenInputCountryEl)) {
          __privateGet(this, _hiddenInputCountryEl).value = getCountryIso2();
        }
      },
      { signal }
    );
  }
  //* Wire up triggers that open/close the dropdown: label click (focus input or swallow repeat click),
  //* selected-country click (open), and keydown on countryContainer (open on arrow/space/enter, close on tab).
  bindAllInitialDropdownListeners(signal, onOpen, onClose) {
    const label = this.telInputEl.closest("label");
    if (label) {
      label.addEventListener(
        "click",
        (e) => {
          if (!this.isDropdownOpen()) {
            this.telInputEl.focus();
          } else {
            e.preventDefault();
          }
        },
        { signal }
      );
    }
    __privateGet(this, _selectedCountryEl).addEventListener(
      "click",
      () => {
        if (!this.isDropdownOpen() && !this.telInputEl.disabled && !this.telInputEl.readOnly) {
          onOpen();
        }
      },
      { signal }
    );
    __privateGet(this, _countryContainerEl).addEventListener(
      "keydown",
      (e) => {
        const openKeys = [
          KEYS.ARROW_UP,
          KEYS.ARROW_DOWN,
          KEYS.SPACE,
          KEYS.ENTER
        ];
        if (!this.isDropdownOpen() && openKeys.includes(e.key)) {
          e.preventDefault();
          e.stopPropagation();
          onOpen();
        }
        if (e.key === KEYS.TAB) {
          onClose();
        }
      },
      { signal }
    );
  }
  //* Open the dropdown: create a fresh AbortController, do the DOM work, and wire up all
  //* dropdown-open listeners (which invoke the caller's onSelect / onClose callbacks).
  openDropdown(onSelect, onClose) {
    const { countrySearch, dropdownAlwaysOpen, dropdownContainer } = __privateGet(this, _options);
    __privateSet(this, _dropdownAbortController, new AbortController());
    this.ensureDropdownWidthSet();
    if (dropdownContainer) {
      __privateMethod(this, __UI_instances, injectAndPositionDetachedDropdown_fn).call(this);
    } else {
      const positionBelow = __privateMethod(this, __UI_instances, shouldPositionInlineDropdownBelowInput_fn).call(this);
      const distance = this.telInputEl.offsetHeight + LAYOUT.DROPDOWN_MARGIN;
      if (positionBelow) {
        __privateGet(this, _dropdownContentEl).style.top = `${distance}px`;
      } else {
        __privateGet(this, _dropdownContentEl).style.bottom = `${distance}px`;
      }
    }
    __privateGet(this, _dropdownContentEl).classList.remove(CLASSES.HIDE);
    __privateGet(this, _selectedCountryEl).setAttribute(ARIA.EXPANDED, "true");
    const itemToHighlight = __privateGet(this, _selectedListItemEl) ?? __privateGet(this, _countryListEl).firstElementChild;
    if (itemToHighlight) {
      __privateMethod(this, __UI_instances, highlightListItem_fn).call(this, itemToHighlight);
    }
    if (countrySearch && !dropdownAlwaysOpen) {
      __privateGet(this, _searchInputEl).focus();
    }
    if (__privateGet(this, _options).useFullscreenPopup && __privateGet(this, _detachedDropdownEl) && window.visualViewport) {
      window.visualViewport.addEventListener(
        "resize",
        () => {
          __privateMethod(this, __UI_instances, adjustFullscreenPopupToViewport_fn).call(this);
          if (__privateGet(this, _highlightedListItemEl)) {
            __privateMethod(this, __UI_instances, scrollCountryListToItem_fn).call(this, __privateGet(this, _highlightedListItemEl));
          }
        },
        { signal: __privateGet(this, _dropdownAbortController).signal }
      );
    }
    __privateGet(this, _dropdownArrowEl).classList.add(CLASSES.ARROW_UP);
    __privateMethod(this, __UI_instances, bindDropdownOpenListeners_fn).call(this, onSelect, onClose);
  }
  // UI: Close the dropdown (DOM + abort dropdown-scoped listeners).
  closeDropdown() {
    const { countrySearch, dropdownContainer } = __privateGet(this, _options);
    __privateGet(this, _dropdownAbortController).abort();
    __privateSet(this, _dropdownAbortController, null);
    __privateGet(this, _dropdownContentEl).classList.add(CLASSES.HIDE);
    __privateGet(this, _selectedCountryEl).setAttribute(ARIA.EXPANDED, "false");
    if (countrySearch) {
      __privateGet(this, _searchInputEl).removeAttribute(ARIA.ACTIVE_DESCENDANT);
      __privateGet(this, _searchInputEl).value = "";
      __privateMethod(this, __UI_instances, applySearchFilter_fn).call(this);
      if (__privateGet(this, _highlightedListItemEl)) {
        __privateGet(this, _highlightedListItemEl).classList.remove(CLASSES.HIGHLIGHT);
        __privateSet(this, _highlightedListItemEl, null);
      }
    }
    __privateGet(this, _dropdownArrowEl).classList.remove(CLASSES.ARROW_UP);
    if (dropdownContainer) {
      __privateGet(this, _detachedDropdownEl).remove();
      __privateGet(this, _detachedDropdownEl).style.top = "";
      __privateGet(this, _detachedDropdownEl).style.bottom = "";
      __privateGet(this, _detachedDropdownEl).style.paddingLeft = "";
      __privateGet(this, _detachedDropdownEl).style.paddingRight = "";
    } else {
      __privateGet(this, _dropdownContentEl).style.top = "";
      __privateGet(this, _dropdownContentEl).style.bottom = "";
    }
  }
  // UI: Whether the dropdown is currently open (visible).
  isDropdownOpen() {
    return !__privateGet(this, _dropdownContentEl).classList.contains(CLASSES.HIDE);
  }
  // Toggle the loading spinner on the selected flag (used during auto-country geoIP lookup).
  setLoading(isLoading) {
    __privateGet(this, _selectedFlagEl).classList.toggle(CLASSES.LOADING, isLoading);
  }
  isLoading() {
    return __privateGet(this, _selectedFlagEl).classList.contains(CLASSES.LOADING);
  }
  // Set the disabled state of the input and dropdown.
  setDisabled(disabled) {
    this.telInputEl.disabled = disabled;
    if (__privateGet(this, _selectedCountryEl)) {
      if (disabled) {
        __privateGet(this, _selectedCountryEl).setAttribute("disabled", "true");
      } else {
        __privateGet(this, _selectedCountryEl).removeAttribute("disabled");
      }
    }
  }
  // Set the readonly state of the input and dropdown.
  setReadonly(readonly) {
    this.telInputEl.readOnly = readonly;
    if (__privateGet(this, _selectedCountryEl)) {
      if (readonly) {
        __privateGet(this, _selectedCountryEl).setAttribute("disabled", "true");
      } else {
        __privateGet(this, _selectedCountryEl).removeAttribute("disabled");
      }
    }
  }
  setCountry(selectedCountryData) {
    const { allowDropdown, showFlags, separateDialCode, i18n } = __privateGet(this, _options);
    const name = selectedCountryData == null ? void 0 : selectedCountryData.name;
    const dialCode = selectedCountryData == null ? void 0 : selectedCountryData.dialCode;
    const iso2 = (selectedCountryData == null ? void 0 : selectedCountryData.iso2) ?? "";
    if (allowDropdown) {
      __privateMethod(this, __UI_instances, updateSelectedListItem_fn).call(this, iso2);
    }
    if (__privateGet(this, _selectedCountryEl)) {
      const flagClass = iso2 && showFlags ? `${CLASSES.FLAG} iti__${iso2}` : `${CLASSES.FLAG} ${CLASSES.GLOBE}`;
      let ariaLabel, title, flagInnerHtml;
      if (iso2) {
        title = name;
        ariaLabel = i18n.selectedCountryAriaLabel.replace("${countryName}", name).replace("${dialCode}", `+${dialCode}`);
        flagInnerHtml = showFlags ? "" : buildGlobeIcon();
      } else {
        title = i18n.noCountrySelected;
        ariaLabel = i18n.noCountrySelected;
        flagInnerHtml = buildGlobeIcon();
      }
      __privateGet(this, _selectedFlagEl).className = flagClass;
      __privateGet(this, _selectedCountryEl).setAttribute("title", title);
      __privateGet(this, _selectedCountryEl).setAttribute(ARIA.LABEL, ariaLabel);
      __privateGet(this, _selectedFlagEl).innerHTML = flagInnerHtml;
    }
    if (separateDialCode) {
      const fullDialCode = dialCode ? `+${dialCode}` : "";
      __privateGet(this, _selectedDialCodeEl).textContent = fullDialCode;
      __privateMethod(this, __UI_instances, updateInputPadding_fn).call(this);
    }
  }
  destroy() {
    this.telInputEl.iti = void 0;
    delete this.telInputEl.dataset[DATA_KEYS.INSTANCE_ID];
    this.telInputEl.style.paddingLeft = __privateGet(this, _originalPaddingLeft);
    const wrapper = this.telInputEl.parentNode;
    if (wrapper) {
      wrapper.before(this.telInputEl);
      wrapper.remove();
    }
    __privateGet(this, _listItemByIso2).clear();
  }
}, _options = new WeakMap(), _id = new WeakMap(), _isRTL = new WeakMap(), _originalPaddingLeft = new WeakMap(), _countries = new WeakMap(), _searchDebounceTimer = new WeakMap(), _inlineDropdownHeight = new WeakMap(), _countryContainerEl = new WeakMap(), _selectedCountryEl = new WeakMap(), _selectedFlagEl = new WeakMap(), _selectedDialCodeEl = new WeakMap(), _dropdownArrowEl = new WeakMap(), _dropdownContentEl = new WeakMap(), _searchIconEl = new WeakMap(), _searchInputEl = new WeakMap(), _searchClearButtonEl = new WeakMap(), _countryListEl = new WeakMap(), _hiddenInputPhoneEl = new WeakMap(), _hiddenInputCountryEl = new WeakMap(), _noResultsMessageEl = new WeakMap(), _searchResultsLiveRegionEl = new WeakMap(), _detachedDropdownEl = new WeakMap(), _selectedListItemEl = new WeakMap(), _highlightedListItemEl = new WeakMap(), _listItemByIso2 = new WeakMap(), _dropdownAbortController = new WeakMap(), __UI_instances = new WeakSet(), createWrapperAndInsert_fn = function() {
  const { allowDropdown, showFlags, containerClass, useFullscreenPopup } = __privateGet(this, _options);
  const parentClasses = buildClassNames({
    iti: true,
    "iti--allow-dropdown": allowDropdown,
    "iti--show-flags": showFlags,
    "iti--inline-dropdown": !useFullscreenPopup,
    [containerClass]: Boolean(containerClass)
  });
  const wrapper = createEl("div", { class: parentClasses });
  if (__privateGet(this, _isRTL)) {
    wrapper.setAttribute("dir", "ltr");
  }
  this.telInputEl.before(wrapper);
  return wrapper;
}, buildCountryContainer_fn = function(wrapper) {
  const { allowDropdown, separateDialCode, showFlags } = __privateGet(this, _options);
  if (!allowDropdown && !showFlags && !separateDialCode) {
    return;
  }
  __privateSet(this, _countryContainerEl, createEl(
    "div",
    // visibly hidden until we measure its width to set the input padding correctly
    { class: `iti__country-container ${CLASSES.V_HIDE}` },
    wrapper
  ));
  if (allowDropdown) {
    __privateSet(this, _selectedCountryEl, createEl(
      "button",
      {
        type: "button",
        class: "iti__selected-country",
        [ARIA.EXPANDED]: "false",
        [ARIA.LABEL]: __privateGet(this, _options).i18n.noCountrySelected,
        [ARIA.HASPOPUP]: "dialog",
        [ARIA.CONTROLS]: `iti-${__privateGet(this, _id)}__dropdown-content`
      },
      __privateGet(this, _countryContainerEl)
    ));
    if (this.telInputEl.disabled) {
      __privateGet(this, _selectedCountryEl).setAttribute("disabled", "true");
    }
  } else {
    __privateSet(this, _selectedCountryEl, createEl(
      "div",
      { class: "iti__selected-country" },
      __privateGet(this, _countryContainerEl)
    ));
  }
  const selectedCountryPrimary = createEl(
    "div",
    { class: "iti__selected-country-primary" },
    __privateGet(this, _selectedCountryEl)
  );
  __privateSet(this, _selectedFlagEl, createEl(
    "div",
    { class: CLASSES.FLAG },
    selectedCountryPrimary
  ));
  if (allowDropdown) {
    __privateSet(this, _dropdownArrowEl, createEl(
      "div",
      { class: "iti__arrow", [ARIA.HIDDEN]: "true" },
      selectedCountryPrimary
    ));
  }
  if (separateDialCode) {
    __privateSet(this, _selectedDialCodeEl, createEl(
      "div",
      { class: "iti__selected-dial-code" },
      __privateGet(this, _selectedCountryEl)
    ));
  }
  if (allowDropdown) {
    __privateMethod(this, __UI_instances, buildDropdownContent_fn).call(this);
  }
}, buildDropdownContent_fn = function() {
  const {
    fixDropdownWidth,
    useFullscreenPopup,
    countrySearch,
    i18n,
    dropdownContainer,
    containerClass
  } = __privateGet(this, _options);
  const extraClasses = fixDropdownWidth ? "" : "iti--flexible-dropdown-width";
  __privateSet(this, _dropdownContentEl, createEl("div", {
    id: `iti-${__privateGet(this, _id)}__dropdown-content`,
    class: `iti__dropdown-content ${CLASSES.HIDE} ${extraClasses}`,
    role: "dialog",
    [ARIA.MODAL]: "true"
  }));
  if (__privateGet(this, _isRTL)) {
    __privateGet(this, _dropdownContentEl).setAttribute("dir", "rtl");
  }
  if (countrySearch) {
    __privateMethod(this, __UI_instances, buildSearchUI_fn).call(this);
  }
  __privateSet(this, _countryListEl, createEl(
    "ul",
    {
      class: "iti__country-list",
      id: `iti-${__privateGet(this, _id)}__country-listbox`,
      role: "listbox",
      [ARIA.LABEL]: i18n.countryListAriaLabel
    },
    __privateGet(this, _dropdownContentEl)
  ));
  __privateMethod(this, __UI_instances, appendListItems_fn).call(this);
  if (countrySearch) {
    __privateMethod(this, __UI_instances, updateSearchResultsA11yText_fn).call(this);
  }
  if (!useFullscreenPopup) {
    __privateSet(this, _inlineDropdownHeight, __privateMethod(this, __UI_instances, getHiddenInlineDropdownHeight_fn).call(this));
    if (countrySearch) {
      __privateGet(this, _dropdownContentEl).style.height = `${__privateGet(this, _inlineDropdownHeight)}px`;
    }
  }
  if (dropdownContainer) {
    const dropdownClasses = buildClassNames({
      iti: true,
      "iti--container": true,
      "iti--fullscreen-popup": useFullscreenPopup,
      "iti--inline-dropdown": !useFullscreenPopup,
      [containerClass]: Boolean(containerClass)
    });
    __privateSet(this, _detachedDropdownEl, createEl("div", { class: dropdownClasses }));
    __privateGet(this, _detachedDropdownEl).appendChild(__privateGet(this, _dropdownContentEl));
  } else {
    __privateGet(this, _countryContainerEl).appendChild(__privateGet(this, _dropdownContentEl));
  }
}, buildSearchUI_fn = function() {
  const { i18n, searchInputClass } = __privateGet(this, _options);
  const searchWrapper = createEl(
    "div",
    { class: "iti__search-input-wrapper" },
    __privateGet(this, _dropdownContentEl)
  );
  __privateSet(this, _searchIconEl, createEl(
    "span",
    {
      class: "iti__search-icon",
      [ARIA.HIDDEN]: "true"
    },
    searchWrapper
  ));
  __privateGet(this, _searchIconEl).innerHTML = buildSearchIcon();
  __privateSet(this, _searchInputEl, createEl(
    "input",
    {
      id: `iti-${__privateGet(this, _id)}__search-input`,
      // Chrome says inputs need either a name or an id
      type: "search",
      class: `iti__search-input ${searchInputClass}`,
      placeholder: i18n.searchPlaceholder,
      // role=combobox + aria-autocomplete=list + aria-activedescendant allows maintaining focus on the search input while allowing users to navigate search results with up/down keyboard keys
      role: "combobox",
      [ARIA.EXPANDED]: "true",
      [ARIA.LABEL]: i18n.searchPlaceholder,
      [ARIA.CONTROLS]: `iti-${__privateGet(this, _id)}__country-listbox`,
      [ARIA.AUTOCOMPLETE]: "list",
      autocomplete: "off"
    },
    searchWrapper
  ));
  __privateSet(this, _searchClearButtonEl, createEl(
    "button",
    {
      type: "button",
      class: `iti__search-clear ${CLASSES.HIDE}`,
      [ARIA.LABEL]: i18n.clearSearchAriaLabel,
      tabindex: "-1"
    },
    searchWrapper
  ));
  __privateGet(this, _searchClearButtonEl).innerHTML = buildClearIcon(__privateGet(this, _id));
  __privateSet(this, _searchResultsLiveRegionEl, createEl(
    "span",
    { class: "iti__a11y-text" },
    __privateGet(this, _dropdownContentEl)
  ));
  __privateSet(this, _noResultsMessageEl, createEl(
    "div",
    {
      class: `iti__no-results ${CLASSES.HIDE}`,
      [ARIA.HIDDEN]: "true"
      // all a11y messaging happens in this.#searchResultsLiveRegionEl
    },
    __privateGet(this, _dropdownContentEl)
  ));
  __privateGet(this, _noResultsMessageEl).textContent = i18n.searchEmptyState ?? null;
}, updateInputPaddingAndReveal_fn = function() {
  if (!__privateGet(this, _countryContainerEl)) {
    return;
  }
  __privateMethod(this, __UI_instances, updateInputPadding_fn).call(this);
  __privateGet(this, _countryContainerEl).classList.remove(CLASSES.V_HIDE);
}, buildHiddenInputs_fn = function(wrapper) {
  var _a4, _b;
  const { hiddenInput } = __privateGet(this, _options);
  if (!hiddenInput) {
    return;
  }
  const telInputName = this.telInputEl.getAttribute("name") || "";
  const names = hiddenInput(telInputName);
  if (names.phone) {
    const existingInput = (_a4 = this.telInputEl.form) == null ? void 0 : _a4.querySelector(
      `input[name="${names.phone}"]`
    );
    if (existingInput) {
      __privateSet(this, _hiddenInputPhoneEl, existingInput);
    } else {
      __privateSet(this, _hiddenInputPhoneEl, createEl("input", {
        type: "hidden",
        name: names.phone
      }));
      wrapper.appendChild(__privateGet(this, _hiddenInputPhoneEl));
    }
  }
  if (names.country) {
    const existingInput = (_b = this.telInputEl.form) == null ? void 0 : _b.querySelector(
      `input[name="${names.country}"]`
    );
    if (existingInput) {
      __privateSet(this, _hiddenInputCountryEl, existingInput);
    } else {
      __privateSet(this, _hiddenInputCountryEl, createEl("input", {
        type: "hidden",
        name: names.country
      }));
      wrapper.appendChild(__privateGet(this, _hiddenInputCountryEl));
    }
  }
}, //* For each country: add a country list item <li> to the countryList <ul> container.
appendListItems_fn = function() {
  const frag = document.createDocumentFragment();
  for (let i = 0; i < __privateGet(this, _countries).length; i++) {
    const c = __privateGet(this, _countries)[i];
    const liClass = buildClassNames({
      [CLASSES.COUNTRY_ITEM]: true
    });
    const listItem = createEl("li", {
      id: `iti-${__privateGet(this, _id)}__item-${c.iso2}`,
      class: liClass,
      tabindex: "-1",
      role: "option",
      [ARIA.SELECTED]: "false"
    });
    listItem.dataset[DATA_KEYS.DIAL_CODE] = c.dialCode;
    listItem.dataset[DATA_KEYS.ISO2] = c.iso2;
    __privateGet(this, _listItemByIso2).set(c.iso2, listItem);
    if (__privateGet(this, _options).showFlags) {
      createEl("div", { class: `${CLASSES.FLAG} iti__${c.iso2}` }, listItem);
    }
    const nameEl = createEl("span", { class: "iti__country-name" }, listItem);
    nameEl.textContent = `${c.name} `;
    const dialEl = createEl("span", { class: "iti__dial-code" }, nameEl);
    if (__privateGet(this, _isRTL)) {
      dialEl.setAttribute("dir", "ltr");
    }
    dialEl.textContent = `(+${c.dialCode})`;
    frag.appendChild(listItem);
  }
  __privateGet(this, _countryListEl).appendChild(frag);
}, //* Update the input padding to make space for (1) the selected country/globe, (2) the arrow, and (3) the separate dial code, all of which are optional, hence handling this in the JS rather than CSS.
updateInputPadding_fn = function() {
  if (__privateGet(this, _selectedCountryEl)) {
    const fallbackWidth = __privateGet(this, _options).separateDialCode ? LAYOUT.FALLBACK_SELECTED_WITH_DIAL_WIDTH : LAYOUT.FALLBACK_SELECTED_NO_DIAL_WIDTH;
    const selectedCountryWidth = __privateGet(this, _selectedCountryEl).offsetWidth || __privateMethod(this, __UI_instances, getHiddenSelectedCountryWidth_fn).call(this) || fallbackWidth;
    const inputPadding = selectedCountryWidth + LAYOUT.INPUT_PADDING_EXTRA_LEFT;
    this.telInputEl.style.paddingLeft = `${inputPadding}px`;
  }
}, __UI_static = new WeakSet(), getBody_fn = function() {
  let body;
  try {
    body = window.top.document.body;
  } catch (e) {
    body = document.body;
  }
  return body;
}, //* When input is in a hidden container during init, we cannot calculate the selected country width.
//* Fix: clone the markup, make it invisible, add it to the end of the DOM, and then measure it's width.
//* To get the right styling to apply, all we need is a shallow clone of the container,
//* and then to inject a deep clone of the selectedCountryEl element.
getHiddenSelectedCountryWidth_fn = function() {
  var _a4;
  if (!this.telInputEl.parentNode) {
    return 0;
  }
  const body = __privateMethod(_a4 = _a, __UI_static, getBody_fn).call(_a4);
  const containerClone = this.telInputEl.parentNode.cloneNode(
    false
  );
  containerClone.style.visibility = "hidden";
  body.appendChild(containerClone);
  const countryContainerClone = __privateGet(this, _countryContainerEl).cloneNode();
  containerClone.appendChild(countryContainerClone);
  const selectedCountryClone = __privateGet(this, _selectedCountryEl).cloneNode(
    true
  );
  countryContainerClone.appendChild(selectedCountryClone);
  const width = selectedCountryClone.offsetWidth;
  body.removeChild(containerClone);
  return width;
}, // Get the dropdown height (before it is added to the DOM)
getHiddenInlineDropdownHeight_fn = function() {
  var _a4;
  const body = __privateMethod(_a4 = _a, __UI_static, getBody_fn).call(_a4);
  __privateGet(this, _dropdownContentEl).classList.remove(CLASSES.HIDE);
  const tempContainer = createEl("div", {
    class: "iti iti--inline-dropdown"
  });
  tempContainer.appendChild(__privateGet(this, _dropdownContentEl));
  tempContainer.style.visibility = "hidden";
  body.appendChild(tempContainer);
  const height = __privateGet(this, _dropdownContentEl).offsetHeight;
  body.removeChild(tempContainer);
  tempContainer.style.visibility = "";
  __privateGet(this, _dropdownContentEl).classList.add(CLASSES.HIDE);
  return height > 0 ? height : LAYOUT.FALLBACK_DROPDOWN_HEIGHT;
}, //* Update search results text (for a11y).
updateSearchResultsA11yText_fn = function() {
  const { i18n } = __privateGet(this, _options);
  const count = __privateGet(this, _countryListEl).childElementCount;
  __privateGet(this, _searchResultsLiveRegionEl).textContent = i18n.searchSummaryAria(count);
}, //* Country search: Filter the countries according to the search query.
filterCountriesByQuery_fn = function(query) {
  let matchedCountries;
  if (query === "") {
    matchedCountries = __privateGet(this, _countries);
  } else {
    matchedCountries = getMatchedCountries(__privateGet(this, _countries), query);
  }
  __privateMethod(this, __UI_instances, showFilteredCountries_fn).call(this, matchedCountries);
}, // Search input handlers
applySearchFilter_fn = function() {
  const inputQuery = __privateGet(this, _searchInputEl).value.trim();
  __privateMethod(this, __UI_instances, filterCountriesByQuery_fn).call(this, inputQuery);
  if (__privateGet(this, _searchInputEl).value) {
    __privateGet(this, _searchClearButtonEl).classList.remove(CLASSES.HIDE);
  } else {
    __privateGet(this, _searchClearButtonEl).classList.add(CLASSES.HIDE);
  }
}, handleSearchChange_fn = function() {
  if (__privateGet(this, _searchDebounceTimer)) {
    clearTimeout(__privateGet(this, _searchDebounceTimer));
  }
  __privateSet(this, _searchDebounceTimer, setTimeout(() => {
    __privateMethod(this, __UI_instances, applySearchFilter_fn).call(this);
    __privateSet(this, _searchDebounceTimer, null);
  }, TIMINGS.SEARCH_DEBOUNCE_MS));
}, handleSearchClear_fn = function() {
  __privateGet(this, _searchInputEl).value = "";
  __privateGet(this, _searchInputEl).focus();
  __privateMethod(this, __UI_instances, applySearchFilter_fn).call(this);
}, //* Check if a country list item element is visible within it's container (the country list), else scroll until it is.
scrollCountryListToItem_fn = function(element) {
  const container = __privateGet(this, _countryListEl);
  const containerRect = container.getBoundingClientRect();
  const elementRect = element.getBoundingClientRect();
  const offsetTop = elementRect.top - containerRect.top + container.scrollTop;
  if (elementRect.top < containerRect.top) {
    container.scrollTop = offsetTop;
  } else if (elementRect.bottom > containerRect.bottom) {
    container.scrollTop = offsetTop - containerRect.height + elementRect.height;
  }
}, //* Remove highlighting from the previous list item and highlight the new one.
highlightListItem_fn = function(listItem, doScroll = true) {
  var _a4;
  (_a4 = __privateGet(this, _highlightedListItemEl)) == null ? void 0 : _a4.classList.remove(CLASSES.HIGHLIGHT);
  if (listItem) {
    listItem.classList.add(CLASSES.HIGHLIGHT);
    if (__privateGet(this, _options).countrySearch) {
      const activeDescendant = listItem.getAttribute("id") || "";
      __privateGet(this, _searchInputEl).setAttribute(
        ARIA.ACTIVE_DESCENDANT,
        activeDescendant
      );
    }
    if (doScroll) {
      __privateMethod(this, __UI_instances, scrollCountryListToItem_fn).call(this, listItem);
    }
    __privateSet(this, _highlightedListItemEl, listItem);
  } else {
    __privateSet(this, _highlightedListItemEl, null);
  }
}, //* Wire up all listeners needed while the dropdown is open: list-item hover (highlight),
//* list-item click & enter key (select), click-off & escape (close), search input (filter),
//* (when countrySearch disabled) typed-char hidden search, and (when dropdown is in an external
//* container) close on window scroll.
bindDropdownOpenListeners_fn = function(onSelect, onClose) {
  const signal = __privateGet(this, _dropdownAbortController).signal;
  __privateMethod(this, __UI_instances, bindListItemHover_fn).call(this, signal);
  __privateMethod(this, __UI_instances, bindListItemClick_fn).call(this, signal, onSelect);
  if (!__privateGet(this, _options).dropdownAlwaysOpen) {
    __privateMethod(this, __UI_instances, bindOutsideClickToClose_fn).call(this, signal, onClose);
  }
  __privateMethod(this, __UI_instances, bindDropdownKeydownListener_fn).call(this, signal, onSelect, onClose);
  if (__privateGet(this, _options).countrySearch) {
    __privateMethod(this, __UI_instances, bindSearchInputListener_fn).call(this, signal);
  }
  if (!__privateGet(this, _options).useFullscreenPopup && __privateGet(this, _options).dropdownContainer) {
    window.addEventListener("scroll", onClose, { signal });
  }
}, //* When mouse over a list item, just highlight that one (so if they hit "enter" we know which to select).
bindListItemHover_fn = function(signal) {
  __privateGet(this, _countryListEl).addEventListener(
    "mouseover",
    (e) => {
      var _a4;
      const listItem = (_a4 = e.target) == null ? void 0 : _a4.closest(
        `.${CLASSES.COUNTRY_ITEM}`
      );
      if (listItem) {
        __privateMethod(this, __UI_instances, highlightListItem_fn).call(this, listItem, false);
      }
    },
    { signal }
  );
}, //* Delegate clicks on the country list to the caller's onSelect callback, passing the clicked list item.
bindListItemClick_fn = function(signal, onSelect) {
  __privateGet(this, _countryListEl).addEventListener(
    "click",
    (e) => {
      var _a4;
      const listItem = (_a4 = e.target) == null ? void 0 : _a4.closest(
        `.${CLASSES.COUNTRY_ITEM}`
      );
      if (listItem) {
        onSelect(listItem);
      }
    },
    { signal }
  );
}, //* Invoke onClickOff when the user clicks anywhere outside the dropdown.
bindOutsideClickToClose_fn = function(signal, onClickOff) {
  setTimeout(() => {
    document.documentElement.addEventListener(
      "click",
      (e) => {
        if (!__privateGet(this, _dropdownContentEl).contains(e.target)) {
          onClickOff();
        }
      },
      { signal }
    );
  }, 0);
}, //* Keyboard navigation while the dropdown is open: arrow keys navigate, hidden-search keys filter,
//* and enter/escape invoke the caller's callbacks (which handle country selection / dropdown close).
//* Listens on document because key events go there when no input has focus.
//* Uses keydown rather than keypress so non-char keys (arrow, esc) fire and so holding a key repeats.
bindDropdownKeydownListener_fn = function(signal, onEnter, onEscape) {
  let query = "";
  let queryTimer = null;
  const handleKeydown = (e) => {
    const allowedKeys = [
      KEYS.ARROW_UP,
      KEYS.ARROW_DOWN,
      KEYS.ENTER,
      KEYS.ESC
    ];
    if (allowedKeys.includes(e.key)) {
      e.preventDefault();
      e.stopPropagation();
      if (e.key === KEYS.ARROW_UP || e.key === KEYS.ARROW_DOWN) {
        __privateMethod(this, __UI_instances, handleUpDownKey_fn).call(this, e.key);
      } else if (e.key === KEYS.ENTER && !e.isComposing) {
        onEnter(__privateGet(this, _highlightedListItemEl));
      } else if (e.key === KEYS.ESC) {
        onEscape();
        __privateGet(this, _selectedCountryEl).focus();
      }
    }
    if (!__privateGet(this, _options).countrySearch && e.target !== this.telInputEl && REGEX.HIDDEN_SEARCH_CHAR.test(e.key)) {
      e.stopPropagation();
      if (queryTimer) {
        clearTimeout(queryTimer);
      }
      query += e.key.toLowerCase();
      __privateMethod(this, __UI_instances, searchForCountry_fn).call(this, query);
      queryTimer = setTimeout(() => {
        query = "";
      }, TIMINGS.HIDDEN_SEARCH_RESET_MS);
    }
  };
  document.addEventListener("keydown", handleKeydown, { signal });
}, //* Wire up country search input listener: typing filters the list, the clear button resets it.
bindSearchInputListener_fn = function(signal) {
  __privateGet(this, _searchInputEl).addEventListener(
    "input",
    () => __privateMethod(this, __UI_instances, handleSearchChange_fn).call(this),
    { signal }
  );
  __privateGet(this, _searchClearButtonEl).addEventListener(
    "click",
    () => __privateMethod(this, __UI_instances, handleSearchClear_fn).call(this),
    { signal }
  );
}, //* Hidden search (countrySearch disabled): jump to the first list item whose name starts with the query.
searchForCountry_fn = function(query) {
  const match = findFirstCountryStartingWith(__privateGet(this, _countries), query);
  if (match) {
    const listItem = __privateGet(this, _listItemByIso2).get(match.iso2);
    __privateMethod(this, __UI_instances, highlightListItem_fn).call(this, listItem);
  }
}, //* Highlight the next/prev item in the list (and ensure it is visible).
handleUpDownKey_fn = function(key) {
  var _a4, _b;
  let next = key === KEYS.ARROW_UP ? (_a4 = __privateGet(this, _highlightedListItemEl)) == null ? void 0 : _a4.previousElementSibling : (_b = __privateGet(this, _highlightedListItemEl)) == null ? void 0 : _b.nextElementSibling;
  if (!next && __privateGet(this, _countryListEl).childElementCount > 1) {
    next = key === KEYS.ARROW_UP ? __privateGet(this, _countryListEl).lastElementChild : __privateGet(this, _countryListEl).firstElementChild;
  }
  if (next) {
    __privateMethod(this, __UI_instances, highlightListItem_fn).call(this, next);
  }
}, // Update the selected list item in the dropdown
updateSelectedListItem_fn = function(iso2) {
  var _a4;
  if (__privateGet(this, _selectedListItemEl) && __privateGet(this, _selectedListItemEl).dataset[DATA_KEYS.ISO2] !== iso2) {
    __privateGet(this, _selectedListItemEl).setAttribute(ARIA.SELECTED, "false");
    (_a4 = __privateGet(this, _selectedListItemEl).querySelector(".iti__country-check")) == null ? void 0 : _a4.remove();
    __privateSet(this, _selectedListItemEl, null);
  }
  if (iso2 && !__privateGet(this, _selectedListItemEl)) {
    const newListItem = __privateGet(this, _countryListEl).querySelector(
      `[data-country-code="${iso2}"]`
    );
    if (newListItem) {
      newListItem.setAttribute(ARIA.SELECTED, "true");
      const checkIcon = createEl(
        "span",
        { class: "iti__country-check", [ARIA.HIDDEN]: "true" },
        newListItem
      );
      checkIcon.innerHTML = buildCheckIcon();
      __privateSet(this, _selectedListItemEl, newListItem);
      if (__privateGet(this, _options).dropdownAlwaysOpen) {
        __privateMethod(this, __UI_instances, highlightListItem_fn).call(this, newListItem);
      }
    }
  }
}, //* Country search: Filter the country list to the given array of countries.
showFilteredCountries_fn = function(matchedCountries) {
  __privateGet(this, _countryListEl).replaceChildren();
  let noCountriesAddedYet = true;
  for (const c of matchedCountries) {
    const listItem = __privateGet(this, _listItemByIso2).get(c.iso2);
    if (listItem) {
      __privateGet(this, _countryListEl).appendChild(listItem);
      if (noCountriesAddedYet) {
        __privateMethod(this, __UI_instances, highlightListItem_fn).call(this, listItem, false);
        noCountriesAddedYet = false;
      }
    }
  }
  if (noCountriesAddedYet) {
    __privateMethod(this, __UI_instances, highlightListItem_fn).call(this, null);
    if (__privateGet(this, _noResultsMessageEl)) {
      __privateGet(this, _noResultsMessageEl).classList.remove(CLASSES.HIDE);
    }
  } else if (__privateGet(this, _noResultsMessageEl)) {
    __privateGet(this, _noResultsMessageEl).classList.add(CLASSES.HIDE);
  }
  __privateGet(this, _countryListEl).scrollTop = 0;
  __privateMethod(this, __UI_instances, updateSearchResultsA11yText_fn).call(this);
}, shouldPositionInlineDropdownBelowInput_fn = function() {
  if (__privateGet(this, _options).dropdownAlwaysOpen) {
    return true;
  }
  const inputPos = this.telInputEl.getBoundingClientRect();
  const spaceAbove = inputPos.top;
  const spaceBelow = window.innerHeight - inputPos.bottom;
  return spaceBelow >= __privateGet(this, _inlineDropdownHeight) || spaceBelow >= spaceAbove;
}, // inject dropdown into container and apply positioning styles
injectAndPositionDetachedDropdown_fn = function() {
  const { dropdownContainer, useFullscreenPopup } = __privateGet(this, _options);
  if (useFullscreenPopup) {
    if (window.innerWidth >= LAYOUT.NARROW_VIEWPORT_WIDTH) {
      const inputPos = this.telInputEl.getBoundingClientRect();
      __privateGet(this, _detachedDropdownEl).style.paddingLeft = `${inputPos.left}px`;
      __privateGet(this, _detachedDropdownEl).style.paddingRight = `${window.innerWidth - inputPos.right}px`;
    }
  } else {
    const inputPos = this.telInputEl.getBoundingClientRect();
    __privateGet(this, _detachedDropdownEl).style.left = `${inputPos.left}px`;
    const positionBelow = __privateMethod(this, __UI_instances, shouldPositionInlineDropdownBelowInput_fn).call(this);
    if (positionBelow) {
      __privateGet(this, _detachedDropdownEl).style.top = `${inputPos.bottom + LAYOUT.DROPDOWN_MARGIN}px`;
    } else {
      __privateGet(this, _detachedDropdownEl).style.top = "unset";
      __privateGet(this, _detachedDropdownEl).style.bottom = `${window.innerHeight - inputPos.top + LAYOUT.DROPDOWN_MARGIN}px`;
    }
  }
  dropdownContainer.appendChild(__privateGet(this, _detachedDropdownEl));
}, // Adjust the fullscreen popup dimensions to match the visual viewport,
// so it stays above the virtual keyboard on mobile devices.
adjustFullscreenPopupToViewport_fn = function() {
  const vv = window.visualViewport;
  if (!vv || !__privateGet(this, _detachedDropdownEl)) {
    return;
  }
  const virtualKeyboardHeight = window.innerHeight - vv.height;
  __privateGet(this, _detachedDropdownEl).style.bottom = `${virtualKeyboardHeight}px`;
}, __privateAdd(_a, __UI_static), _a);
var processAllCountries = (options) => {
  const { onlyCountries, excludeCountries } = options;
  if (onlyCountries == null ? void 0 : onlyCountries.length) {
    return data_default.filter(
      (country) => onlyCountries.includes(country.iso2)
    );
  } else if (excludeCountries == null ? void 0 : excludeCountries.length) {
    return data_default.filter(
      (country) => !excludeCountries.includes(country.iso2)
    );
  }
  return [...data_default];
};
var generateCountryNames = (countries, options) => {
  const { countryNameLocale, i18n } = options;
  let displayNames;
  try {
    const hasDisplayNames = typeof Intl !== "undefined" && typeof Intl.DisplayNames === "function";
    if (hasDisplayNames) {
      displayNames = new Intl.DisplayNames(countryNameLocale, {
        type: "region"
      });
    } else {
      displayNames = null;
    }
  } catch (e) {
    console.error(e);
    displayNames = null;
  }
  for (const c of countries) {
    c.name = i18n[c.iso2] || (displayNames == null ? void 0 : displayNames.of(c.iso2.toUpperCase())) || "";
  }
};
var processDialCodes = (countries) => {
  const dialCodes = /* @__PURE__ */ new Set();
  let dialCodeMaxLength = 0;
  const dialCodeToIso2Map = {};
  const addToDialCodeMap = (iso2, dialCode) => {
    if (!iso2 || !dialCode) {
      return;
    }
    if (dialCode.length > dialCodeMaxLength) {
      dialCodeMaxLength = dialCode.length;
    }
    if (!Object.hasOwn(dialCodeToIso2Map, dialCode)) {
      dialCodeToIso2Map[dialCode] = [];
    }
    const iso2List = dialCodeToIso2Map[dialCode];
    if (iso2List.includes(iso2)) {
      return;
    }
    iso2List.push(iso2);
  };
  const countriesSortedByPriority = [...countries].sort(
    (a, b) => a.priority - b.priority
  );
  for (const c of countriesSortedByPriority) {
    if (!dialCodes.has(c.dialCode)) {
      dialCodes.add(c.dialCode);
    }
    for (let k = 1; k < c.dialCode.length; k++) {
      const partialDialCode = c.dialCode.substring(0, k);
      addToDialCodeMap(c.iso2, partialDialCode);
    }
    addToDialCodeMap(c.iso2, c.dialCode);
    if (c.areaCodes) {
      const rootIso2Code = dialCodeToIso2Map[c.dialCode][0];
      for (const areaCode of c.areaCodes) {
        for (let k = 1; k < areaCode.length; k++) {
          const partialAreaCode = areaCode.substring(0, k);
          const partialDialCode = c.dialCode + partialAreaCode;
          addToDialCodeMap(rootIso2Code, partialDialCode);
          addToDialCodeMap(c.iso2, partialDialCode);
        }
        addToDialCodeMap(c.iso2, c.dialCode + areaCode);
      }
    }
  }
  return { dialCodes, dialCodeMaxLength, dialCodeToIso2Map };
};
var sortCountries = (countries, options) => {
  const { countryOrder } = options;
  countries.sort((a, b) => {
    if (countryOrder) {
      const aIndex = countryOrder.indexOf(a.iso2);
      const bIndex = countryOrder.indexOf(b.iso2);
      const aIndexExists = aIndex > -1;
      const bIndexExists = bIndex > -1;
      if (aIndexExists || bIndexExists) {
        if (aIndexExists && bIndexExists) {
          return aIndex - bIndex;
        }
        return aIndexExists ? -1 : 1;
      }
    }
    return a.name.localeCompare(b.name);
  });
};
var cacheSearchTokens = (countries) => {
  for (const c of countries) {
    c.normalisedName = normaliseString(c.name);
    c.initials = c.normalisedName.split(/[^a-z]/).map((word) => word[0]).join("");
    c.dialCodePlus = `+${c.dialCode}`;
  }
};
var regionlessDialCodes = /* @__PURE__ */ new Set([
  "800",
  "808",
  "870",
  "881",
  "882",
  "883",
  "888",
  "979"
]);
var hasRegionlessDialCode = (number) => {
  const dialCode = getNumeric(number).slice(0, 3);
  return number.startsWith("+") && regionlessDialCodes.has(dialCode);
};
var stripSeparateDialCode = (fullNumber, hasValidDialCode, separateDialCode, selectedCountryData) => {
  if (!separateDialCode || !hasValidDialCode) {
    return fullNumber;
  }
  const dialCode = `+${selectedCountryData.dialCode}`;
  const start = fullNumber[dialCode.length] === " " || fullNumber[dialCode.length] === "-" ? dialCode.length + 1 : dialCode.length;
  return fullNumber.substring(start);
};
var formatNumberAsYouType = (fullNumber, telInputValue, utils, selectedCountryData, separateDialCode) => {
  const result = utils ? utils.formatNumberAsYouType(fullNumber, selectedCountryData == null ? void 0 : selectedCountryData.iso2) : fullNumber;
  const dialCode = selectedCountryData == null ? void 0 : selectedCountryData.dialCode;
  if (separateDialCode && telInputValue.charAt(0) !== "+" && result.includes(`+${dialCode}`)) {
    const afterDialCode = result.split(`+${dialCode}`)[1] || "";
    return afterDialCode.trim();
  }
  return result;
};
var computeNewCaretPosition = (relevantChars, formattedValue, prevCaretPos, isDeleteForwards) => {
  if (prevCaretPos === 0 && !isDeleteForwards) {
    return 0;
  }
  let relevantCharCount = 0;
  for (let i = 0; i < formattedValue.length; i++) {
    if (/[+0-9]/.test(formattedValue[i])) {
      relevantCharCount++;
    }
    if (relevantCharCount === relevantChars && !isDeleteForwards) {
      return i + 1;
    }
    if (isDeleteForwards && relevantCharCount === relevantChars + 1) {
      return i;
    }
  }
  return formattedValue.length;
};
var regionlessNanpAreaCodes = /* @__PURE__ */ new Set([
  "800",
  "822",
  "833",
  "844",
  "855",
  "866",
  "877",
  "880",
  "881",
  "882",
  "883",
  "884",
  "885",
  "886",
  "887",
  "888",
  "889"
]);
var isRegionlessNanp = (number) => {
  const numeric = getNumeric(number);
  if (numeric.startsWith(DIAL_CODE.NANP) && numeric.length >= 4) {
    const areaCode = numeric.substring(1, 4);
    return regionlessNanpAreaCodes.has(areaCode);
  }
  return false;
};
var _userNumeralSet, _Numerals_instances, updateNumeralSet_fn, _a2;
var Numerals = (_a2 = class {
  constructor(initialValue) {
    __privateAdd(this, _Numerals_instances);
    __privateAdd(this, _userNumeralSet);
    if (initialValue) {
      __privateMethod(this, _Numerals_instances, updateNumeralSet_fn).call(this, initialValue);
    }
  }
  // Denormalise ASCII 0-9 to the user's numeral set. If not yet known, return as-is.
  // NOTE: normalise is always called before this, so it should be impossible for the numeral set to be unknown at this point.
  denormalise(str) {
    if (!__privateGet(this, _userNumeralSet) || __privateGet(this, _userNumeralSet) === "ascii") {
      return str;
    }
    const base = __privateGet(this, _userNumeralSet) === "arabic-indic" ? 1632 : 1776;
    return str.replace(/[0-9]/g, (d) => String.fromCharCode(base + Number(d)));
  }
  // Normalize Eastern Arabic (U+0660-0669) and Persian/Extended Arabic-Indic (U+06F0-06F9) numerals to ASCII 0-9
  normalise(str) {
    if (!str) {
      return "";
    }
    __privateMethod(this, _Numerals_instances, updateNumeralSet_fn).call(this, str);
    if (__privateGet(this, _userNumeralSet) === "ascii") {
      return str;
    }
    const base = __privateGet(this, _userNumeralSet) === "arabic-indic" ? 1632 : 1776;
    const regex = __privateGet(this, _userNumeralSet) === "arabic-indic" ? /[\u0660-\u0669]/g : /[\u06F0-\u06F9]/g;
    return str.replace(
      regex,
      (ch) => String.fromCharCode(48 + (ch.charCodeAt(0) - base))
    );
  }
  isAscii() {
    return !__privateGet(this, _userNumeralSet) || __privateGet(this, _userNumeralSet) === "ascii";
  }
}, _userNumeralSet = new WeakMap(), _Numerals_instances = new WeakSet(), // If any Arabic-Indic digits, then label it as that set. Same for Persian. Otherwise assume ASCII.
updateNumeralSet_fn = function(str) {
  if (/[\u0660-\u0669]/.test(str)) {
    __privateSet(this, _userNumeralSet, "arabic-indic");
  } else if (/[\u06F0-\u06F9]/.test(str)) {
    __privateSet(this, _userNumeralSet, "persian");
  } else {
    __privateSet(this, _userNumeralSet, "ascii");
  }
}, _a2);
var nextId = 0;
var ensureUtils = (methodName) => {
  if (!intlTelInput.utils) {
    throw new Error(
      `intlTelInput.utils is required for ${methodName}(). See: https://intl-tel-input.com/docs/utils`
    );
  }
};
var createDeferred = () => {
  let resolve;
  let reject;
  const promise = new Promise((res, rej) => {
    resolve = res;
    reject = rej;
  });
  return { promise, resolve, reject };
};
var _ui, _options2, _isAndroid, _countries2, _dialCodeMaxLength, _dialCodeToIso2Map, _dialCodes, _countryByIso2, _selectedCountry, _maxCoreNumberLength, _fallbackCountryIso2, _isActive, _abortController, _numerals, _userOverrideFormatting, _autoCountryDeferred, _utilsDeferred, __Iti_instances, getTelInputValue_fn, setTelInputValue_fn, createInitPromise_fn, init_fn, processCountryData_fn, setInitialState_fn, initListeners_fn, startAsyncLoads_fn, loadAutoCountry_fn, openDropdownWithPlus_fn, removeJustTypedChar_fn, bindAllTelInputListeners_fn, handleAndroidPlusKey_fn, handleAndroidStrictReject_fn, formatAsYouType_fn, stripTypedDialCode_fn, bindInputListener_fn, _handleInputEvent, bindKeydownListener_fn, _handleKeydownEvent, bindPasteListener_fn, _handlePasteEvent, truncateToMaxLength_fn, dispatchEvent_fn, openDropdown_fn, updateValueFromNumber_fn, updateCountryFromNumber_fn, withDialCodePrefix_fn, resolveCountryChangeFromNumber_fn, resolveCountryChangeFromMultiMatch_fn, updateSelectedCountry_fn, updateMaxCoreNumberLength_fn, updatePlaceholder_fn, selectListItem_fn, closeDropdown_fn, updateDialCode_fn, getDialCode_fn, buildFullNumber_fn, getFullNumber_fn, prepareNumberForInput_fn, getPublicCountryData_fn, dispatchCountryChangeEvent_fn, handleAutoCountryLoaded_fn, handleAutoCountryFailure_fn, handleUtilsLoaded_fn, handleUtilsFailure_fn, validateNumber_fn, _a3;
var Iti = (_a3 = class {
  constructor(input, customOptions = {}) {
    __privateAdd(this, __Iti_instances);
    //* PUBLIC FIELDS - READONLY
    //* Can't be private as it's called from intlTelInput convenience wrapper.
    __publicField(this, "id");
    // accessed externally via iti.promise.then(...)
    __publicField(this, "promise");
    //* PRIVATE FIELDS
    __privateAdd(this, _ui);
    __privateAdd(this, _options2);
    __privateAdd(this, _isAndroid);
    // country data
    __privateAdd(this, _countries2);
    __privateAdd(this, _dialCodeMaxLength);
    __privateAdd(this, _dialCodeToIso2Map);
    __privateAdd(this, _dialCodes);
    __privateAdd(this, _countryByIso2);
    __privateAdd(this, _selectedCountry, null);
    __privateAdd(this, _maxCoreNumberLength, null);
    __privateAdd(this, _fallbackCountryIso2);
    // is this instance still active (not destroyed)
    __privateAdd(this, _isActive, true);
    __privateAdd(this, _abortController);
    __privateAdd(this, _numerals);
    //* Tracks whether the user has typed/pasted their own formatting chars, so AYT-formatting should back off.
    __privateAdd(this, _userOverrideFormatting, false);
    __privateAdd(this, _autoCountryDeferred);
    __privateAdd(this, _utilsDeferred);
    //* On input event: (1) Update selected country, (2) Format-as-you-type.
    //* Note that this fires AFTER the input is updated.
    __privateAdd(this, _handleInputEvent, (e) => {
      const { strictMode, formatAsYouType, separateDialCode, allowDropdown, countrySearch } = __privateGet(this, _options2);
      const detail = e == null ? void 0 : e.detail;
      if (detail == null ? void 0 : detail["isCountryChange"]) {
        return;
      }
      const inputValue = __privateMethod(this, __Iti_instances, getTelInputValue_fn).call(this);
      if (__privateGet(this, _isAndroid) && (e == null ? void 0 : e.data) === "+" && separateDialCode && allowDropdown && countrySearch) {
        __privateMethod(this, __Iti_instances, handleAndroidPlusKey_fn).call(this, inputValue);
        return;
      }
      if (__privateGet(this, _isAndroid) && strictMode && ((e == null ? void 0 : e.data) === " " || (e == null ? void 0 : e.data) === "-" || (e == null ? void 0 : e.data) === ".")) {
        __privateMethod(this, __Iti_instances, handleAndroidStrictReject_fn).call(this, inputValue, e.data);
        return;
      }
      if (__privateMethod(this, __Iti_instances, updateCountryFromNumber_fn).call(this, inputValue)) {
        __privateMethod(this, __Iti_instances, dispatchCountryChangeEvent_fn).call(this);
      }
      const isFormattingChar = (e == null ? void 0 : e.data) && REGEX.NON_PLUS_NUMERIC.test(e.data);
      const isPaste = (e == null ? void 0 : e.inputType) === INPUT_TYPES.PASTE && inputValue;
      if (isFormattingChar || isPaste && !strictMode) {
        __privateSet(this, _userOverrideFormatting, true);
      } else if (!REGEX.NON_PLUS_NUMERIC.test(inputValue)) {
        __privateSet(this, _userOverrideFormatting, false);
      }
      if (formatAsYouType && !__privateGet(this, _userOverrideFormatting) && !(detail == null ? void 0 : detail["isSetNumber"]) && __privateGet(this, _numerals).isAscii()) {
        __privateMethod(this, __Iti_instances, formatAsYouType_fn).call(this, inputValue, (e == null ? void 0 : e.inputType) === INPUT_TYPES.DELETE_FORWARD);
      }
      if (separateDialCode) {
        __privateMethod(this, __Iti_instances, stripTypedDialCode_fn).call(this, inputValue);
      }
    });
    //* On keydown event: (1) if strictMode then prevent invalid characters, (2) if separateDialCode then handle plus key
    //* Note that this fires BEFORE the input is updated.
    __privateAdd(this, _handleKeydownEvent, (e) => {
      var _a4;
      const { strictMode, separateDialCode, allowDropdown, countrySearch } = __privateGet(this, _options2);
      if (!e.key || e.key.length !== 1 || e.altKey || e.ctrlKey || e.metaKey) {
        return;
      }
      if (separateDialCode && allowDropdown && countrySearch && e.key === "+") {
        e.preventDefault();
        __privateMethod(this, __Iti_instances, openDropdownWithPlus_fn).call(this);
        return;
      }
      if (!strictMode) {
        return;
      }
      const inputValue = __privateMethod(this, __Iti_instances, getTelInputValue_fn).call(this);
      const alreadyHasPlus = inputValue.startsWith("+");
      const isInitialPlus = !alreadyHasPlus && __privateGet(this, _ui).telInputEl.selectionStart === 0 && e.key === "+";
      const normalisedKey = __privateGet(this, _numerals).normalise(e.key);
      const isNumeric = /^[0-9]$/.test(normalisedKey);
      const isAllowedChar = separateDialCode ? isNumeric : isInitialPlus || isNumeric;
      const input = __privateGet(this, _ui).telInputEl;
      const selStart = input.selectionStart;
      const selEnd = input.selectionEnd;
      const before = inputValue.slice(0, selStart ?? void 0);
      const after = inputValue.slice(selEnd ?? void 0);
      const newValue = before + normalisedKey + after;
      const newFullNumber = __privateMethod(this, __Iti_instances, buildFullNumber_fn).call(this, newValue);
      let hasExceededMaxLength = false;
      if (intlTelInput.utils && __privateGet(this, _maxCoreNumberLength)) {
        const coreNumber = intlTelInput.utils.getCoreNumber(
          newFullNumber,
          (_a4 = __privateGet(this, _selectedCountry)) == null ? void 0 : _a4.iso2
        );
        hasExceededMaxLength = coreNumber.length > __privateGet(this, _maxCoreNumberLength);
      }
      const newCountry = __privateMethod(this, __Iti_instances, resolveCountryChangeFromNumber_fn).call(this, newFullNumber);
      const isChangingDialCode = newCountry !== null;
      if (!isAllowedChar || hasExceededMaxLength && !isChangingDialCode && !isInitialPlus) {
        __privateMethod(this, __Iti_instances, dispatchEvent_fn).call(this, EVENTS.STRICT_REJECT, {
          source: "key",
          rejectedInput: e.key,
          reason: !isAllowedChar ? "invalid" : "max-length"
        });
        e.preventDefault();
      }
    });
    __privateAdd(this, _handlePasteEvent, (e) => {
      var _a4;
      e.preventDefault();
      const input = __privateGet(this, _ui).telInputEl;
      const selStart = input.selectionStart;
      const selEnd = input.selectionEnd;
      const inputValue = __privateMethod(this, __Iti_instances, getTelInputValue_fn).call(this);
      const before = inputValue.slice(0, selStart ?? void 0);
      const after = inputValue.slice(selEnd ?? void 0);
      const iso2 = (_a4 = __privateGet(this, _selectedCountry)) == null ? void 0 : _a4.iso2;
      const pastedRaw = e.clipboardData.getData("text");
      const pasted = __privateGet(this, _numerals).normalise(pastedRaw);
      const initialCharSelected = selStart === 0 && selEnd > 0;
      const allowLeadingPlus = !inputValue.startsWith("+") || initialCharSelected;
      const allowedChars = pasted.replace(REGEX.NON_PLUS_NUMERIC_GLOBAL, "");
      const hasLeadingPlus = allowedChars.startsWith("+");
      const numerics = allowedChars.replace(/\+/g, "");
      const sanitised = hasLeadingPlus && allowLeadingPlus ? `+${numerics}` : numerics;
      let newValue = before + sanitised + after;
      let rejectReason = sanitised !== pasted ? "invalid" : null;
      if (newValue.length > 5 && intlTelInput.utils) {
        let coreNumber = intlTelInput.utils.getCoreNumber(newValue, iso2);
        while (coreNumber.length === 0 && newValue.length > 0) {
          newValue = newValue.slice(0, -1);
          coreNumber = intlTelInput.utils.getCoreNumber(newValue, iso2);
        }
        if (!coreNumber) {
          __privateMethod(this, __Iti_instances, dispatchEvent_fn).call(this, EVENTS.STRICT_REJECT, {
            source: "paste",
            rejectedInput: pastedRaw,
            reason: "max-length"
          });
          return;
        }
        if (__privateGet(this, _maxCoreNumberLength) && coreNumber.length > __privateGet(this, _maxCoreNumberLength)) {
          if (input.selectionEnd === inputValue.length) {
            const trimLength = coreNumber.length - __privateGet(this, _maxCoreNumberLength);
            newValue = newValue.slice(0, newValue.length - trimLength);
            rejectReason = "max-length";
          } else {
            __privateMethod(this, __Iti_instances, dispatchEvent_fn).call(this, EVENTS.STRICT_REJECT, {
              source: "paste",
              rejectedInput: pastedRaw,
              reason: "max-length"
            });
            return;
          }
        }
      }
      __privateMethod(this, __Iti_instances, setTelInputValue_fn).call(this, newValue);
      const caretPos = selStart + sanitised.length;
      input.setSelectionRange(caretPos, caretPos);
      input.dispatchEvent(new InputEvent("input", { bubbles: true }));
      if (rejectReason) {
        __privateMethod(this, __Iti_instances, dispatchEvent_fn).call(this, EVENTS.STRICT_REJECT, {
          source: "paste",
          rejectedInput: pastedRaw,
          reason: rejectReason
        });
      }
    });
    this.id = nextId++;
    UI.validateInput(input);
    const validatedOptions = validateOptions(customOptions);
    __privateSet(this, _options2, { ...defaults, ...validatedOptions });
    normaliseOptions(__privateGet(this, _options2));
    applyOptionSideEffects(__privateGet(this, _options2));
    __privateSet(this, _ui, new UI(input, __privateGet(this, _options2), this.id));
    __privateSet(this, _isAndroid, typeof navigator !== "undefined" && /Android/i.test(navigator.userAgent));
    __privateSet(this, _numerals, new Numerals(input.value));
    this.promise = __privateMethod(this, __Iti_instances, createInitPromise_fn).call(this, __privateGet(this, _options2));
    __privateSet(this, _countries2, processAllCountries(__privateGet(this, _options2)));
    const { dialCodes, dialCodeMaxLength, dialCodeToIso2Map } = processDialCodes(
      __privateGet(this, _countries2)
    );
    __privateSet(this, _dialCodes, dialCodes);
    __privateSet(this, _dialCodeMaxLength, dialCodeMaxLength);
    __privateSet(this, _dialCodeToIso2Map, dialCodeToIso2Map);
    __privateSet(this, _countryByIso2, new Map(__privateGet(this, _countries2).map((c) => [c.iso2, c])));
    __privateMethod(this, __Iti_instances, init_fn).call(this);
  }
  //********************
  //*  PUBLIC METHODS
  //********************
  //* Remove plugin.
  destroy() {
    if (!__privateGet(this, _isActive)) {
      return;
    }
    __privateSet(this, _isActive, false);
    if (__privateGet(this, _options2).allowDropdown) {
      __privateMethod(this, __Iti_instances, closeDropdown_fn).call(this, true);
    }
    __privateGet(this, _abortController).abort();
    __privateGet(this, _ui).destroy();
    intlTelInput.instances.delete(String(this.id));
  }
  // check if the instance is still valid (not destroyed)
  isActive() {
    return __privateGet(this, _isActive);
  }
  //* Get the extension from the current number.
  getExtension() {
    var _a4;
    if (!__privateGet(this, _isActive)) {
      return "";
    }
    ensureUtils("getExtension");
    return intlTelInput.utils.getExtension(
      __privateMethod(this, __Iti_instances, getFullNumber_fn).call(this),
      (_a4 = __privateGet(this, _selectedCountry)) == null ? void 0 : _a4.iso2
    );
  }
  //* Format the number to the given format.
  getNumber(format) {
    var _a4;
    if (!__privateGet(this, _isActive)) {
      return "";
    }
    ensureUtils("getNumber");
    const iso2 = (_a4 = __privateGet(this, _selectedCountry)) == null ? void 0 : _a4.iso2;
    const fullNumber = __privateMethod(this, __Iti_instances, getFullNumber_fn).call(this);
    const formattedNumber = intlTelInput.utils.formatNumber(
      fullNumber,
      iso2,
      format
    );
    return __privateGet(this, _numerals).denormalise(formattedNumber);
  }
  //* Get the type of the entered number e.g. landline/mobile.
  getNumberType() {
    var _a4;
    if (!__privateGet(this, _isActive)) {
      return SENTINELS.UNKNOWN_NUMBER_TYPE;
    }
    ensureUtils("getNumberType");
    return intlTelInput.utils.getNumberType(
      __privateMethod(this, __Iti_instances, getFullNumber_fn).call(this),
      (_a4 = __privateGet(this, _selectedCountry)) == null ? void 0 : _a4.iso2
    );
  }
  //* Get the country data for the currently selected country.
  getSelectedCountryData() {
    return __privateMethod(this, __Iti_instances, getPublicCountryData_fn).call(this);
  }
  //* Get the validation error.
  getValidationError() {
    var _a4;
    if (!__privateGet(this, _isActive)) {
      return SENTINELS.UNKNOWN_VALIDATION_ERROR;
    }
    ensureUtils("getValidationError");
    const iso2 = (_a4 = __privateGet(this, _selectedCountry)) == null ? void 0 : _a4.iso2;
    return intlTelInput.utils.getValidationError(__privateMethod(this, __Iti_instances, getFullNumber_fn).call(this), iso2);
  }
  //* Validate the input value using number length only
  isValidNumber() {
    var _a4, _b;
    if (!__privateGet(this, _isActive)) {
      return null;
    }
    ensureUtils("isValidNumber");
    const dialCode = (_a4 = __privateGet(this, _selectedCountry)) == null ? void 0 : _a4.dialCode;
    const iso2 = (_b = __privateGet(this, _selectedCountry)) == null ? void 0 : _b.iso2;
    const number = __privateMethod(this, __Iti_instances, getFullNumber_fn).call(this);
    const coreNumber = intlTelInput.utils.getCoreNumber(number, iso2);
    if (coreNumber) {
      if (dialCode === UK.DIAL_CODE) {
        if (coreNumber[0] === UK.MOBILE_PREFIX && coreNumber.length !== UK.MOBILE_CORE_LENGTH) {
          return false;
        }
      }
      const hasAlphaChar = REGEX.ALPHA_UNICODE.test(number);
      if (!hasAlphaChar && dialCode) {
        const nationalPortion = number.startsWith("+") ? number.slice(1 + dialCode.length) : number;
        const nationalDigitCount = getNumeric(nationalPortion).length;
        if (coreNumber.length > nationalDigitCount) {
          return false;
        }
      }
    }
    return __privateMethod(this, __Iti_instances, validateNumber_fn).call(this, "possible");
  }
  //* Validate the input value with precise validation
  isValidNumberPrecise() {
    if (!__privateGet(this, _isActive)) {
      return null;
    }
    ensureUtils("isValidNumberPrecise");
    return __privateMethod(this, __Iti_instances, validateNumber_fn).call(this, "precise");
  }
  //* Update the selected country, and update the input value accordingly.
  setCountry(iso2) {
    var _a4, _b;
    if (!__privateGet(this, _isActive)) {
      return;
    }
    const iso2Lower = iso2 == null ? void 0 : iso2.toLowerCase();
    if (!isIso2(iso2Lower)) {
      throw new Error(`Invalid iso2 code: '${iso2Lower}'`);
    }
    const currentCountry = (_a4 = __privateGet(this, _selectedCountry)) == null ? void 0 : _a4.iso2;
    const isCountryChange = iso2 && iso2Lower !== currentCountry || !iso2 && currentCountry;
    if (!isCountryChange) {
      return;
    }
    __privateMethod(this, __Iti_instances, updateSelectedCountry_fn).call(this, iso2Lower);
    __privateMethod(this, __Iti_instances, updateDialCode_fn).call(this, ((_b = __privateGet(this, _selectedCountry)) == null ? void 0 : _b.dialCode) || "");
    if (__privateGet(this, _options2).formatOnDisplay) {
      const inputValue = __privateMethod(this, __Iti_instances, getTelInputValue_fn).call(this);
      __privateMethod(this, __Iti_instances, updateValueFromNumber_fn).call(this, inputValue);
    }
    __privateMethod(this, __Iti_instances, dispatchCountryChangeEvent_fn).call(this);
    __privateMethod(this, __Iti_instances, dispatchEvent_fn).call(this, EVENTS.INPUT, { isCountryChange: true });
  }
  //* Set the input value and update the country.
  setNumber(number) {
    if (!__privateGet(this, _isActive)) {
      return;
    }
    const normalisedNumber = __privateGet(this, _numerals).normalise(number);
    const countryChanged = __privateMethod(this, __Iti_instances, updateCountryFromNumber_fn).call(this, normalisedNumber);
    __privateMethod(this, __Iti_instances, updateValueFromNumber_fn).call(this, normalisedNumber);
    if (countryChanged) {
      __privateMethod(this, __Iti_instances, dispatchCountryChangeEvent_fn).call(this);
    }
    __privateMethod(this, __Iti_instances, dispatchEvent_fn).call(this, EVENTS.INPUT, { isSetNumber: true });
  }
  //* Set the placeholder number type
  setPlaceholderNumberType(type) {
    if (!__privateGet(this, _isActive)) {
      return;
    }
    __privateGet(this, _options2).placeholderNumberType = type;
    __privateMethod(this, __Iti_instances, updatePlaceholder_fn).call(this);
  }
  // Set the disabled state of the input and dropdown.
  setDisabled(disabled) {
    if (!__privateGet(this, _isActive)) {
      return;
    }
    __privateGet(this, _ui).setDisabled(disabled);
  }
  // Set the readonly state of the input and dropdown.
  setReadonly(readonly) {
    if (!__privateGet(this, _isActive)) {
      return;
    }
    __privateGet(this, _ui).setReadonly(readonly);
  }
  //********************
  //*  STATIC METHODS
  //********************
  // Internal instance notification used by utils/geoip loaders.
  // Kept public so module-level helpers (e.g. attachUtils) can call it, while still allowing
  // access to private instance methods.
  static forEachInstance(method, ...args) {
    const values = [...intlTelInput.instances.values()];
    const arg = args[0];
    values.forEach((instance) => {
      var _a4, _b, _c, _d;
      if (!(instance instanceof _a3)) {
        return;
      }
      switch (method) {
        case "handleUtilsLoaded":
          __privateMethod(_a4 = instance, __Iti_instances, handleUtilsLoaded_fn).call(_a4);
          break;
        case "handleUtilsFailure":
          __privateMethod(_b = instance, __Iti_instances, handleUtilsFailure_fn).call(_b, arg);
          break;
        case "handleAutoCountryLoaded":
          __privateMethod(_c = instance, __Iti_instances, handleAutoCountryLoaded_fn).call(_c);
          break;
        case "handleAutoCountryFailure":
          __privateMethod(_d = instance, __Iti_instances, handleAutoCountryFailure_fn).call(_d);
          break;
      }
    });
  }
}, _ui = new WeakMap(), _options2 = new WeakMap(), _isAndroid = new WeakMap(), _countries2 = new WeakMap(), _dialCodeMaxLength = new WeakMap(), _dialCodeToIso2Map = new WeakMap(), _dialCodes = new WeakMap(), _countryByIso2 = new WeakMap(), _selectedCountry = new WeakMap(), _maxCoreNumberLength = new WeakMap(), _fallbackCountryIso2 = new WeakMap(), _isActive = new WeakMap(), _abortController = new WeakMap(), _numerals = new WeakMap(), _userOverrideFormatting = new WeakMap(), _autoCountryDeferred = new WeakMap(), _utilsDeferred = new WeakMap(), __Iti_instances = new WeakSet(), getTelInputValue_fn = function() {
  const inputValue = __privateGet(this, _ui).telInputEl.value.trim();
  return __privateGet(this, _numerals).normalise(inputValue);
}, setTelInputValue_fn = function(asciiValue) {
  __privateGet(this, _ui).telInputEl.value = __privateGet(this, _numerals).denormalise(asciiValue);
}, createInitPromise_fn = function(options) {
  var _a4, _b;
  const { initialCountry, geoIpLookup, loadUtils } = options;
  const needsAutoCountryDeferred = initialCountry === INITIAL_COUNTRY.AUTO && Boolean(geoIpLookup);
  const needsUtilsDeferred = Boolean(loadUtils) && !intlTelInput.utils;
  if (needsAutoCountryDeferred) {
    __privateSet(this, _autoCountryDeferred, createDeferred());
  }
  if (needsUtilsDeferred) {
    __privateSet(this, _utilsDeferred, createDeferred());
  }
  return Promise.all([
    (_a4 = __privateGet(this, _autoCountryDeferred)) == null ? void 0 : _a4.promise,
    (_b = __privateGet(this, _utilsDeferred)) == null ? void 0 : _b.promise
  ]).then(() => {
  });
}, init_fn = function() {
  __privateSet(this, _abortController, new AbortController());
  __privateMethod(this, __Iti_instances, processCountryData_fn).call(this);
  __privateGet(this, _ui).buildMarkup(__privateGet(this, _countries2));
  __privateMethod(this, __Iti_instances, setInitialState_fn).call(this);
  __privateMethod(this, __Iti_instances, initListeners_fn).call(this);
  __privateMethod(this, __Iti_instances, startAsyncLoads_fn).call(this);
  if (__privateGet(this, _options2).dropdownAlwaysOpen) {
    __privateMethod(this, __Iti_instances, openDropdown_fn).call(this);
  }
}, //********************
//*  PRIVATE METHODS
//********************
//* Prepare all of the country data, including onlyCountries, excludeCountries, countryOrder options.
processCountryData_fn = function() {
  generateCountryNames(__privateGet(this, _countries2), __privateGet(this, _options2));
  sortCountries(__privateGet(this, _countries2), __privateGet(this, _options2));
  cacheSearchTokens(__privateGet(this, _countries2));
}, //* Set the initial state of the input value and the selected country by:
//* 1. Extracting a dial code from the given number
//* 2. Using explicit initialCountry
setInitialState_fn = function(overrideAutoCountry = false) {
  const attributeValueRaw = __privateGet(this, _ui).telInputEl.getAttribute("value");
  const attributeValue = __privateGet(this, _numerals).normalise(attributeValueRaw ?? "");
  const inputValue = __privateMethod(this, __Iti_instances, getTelInputValue_fn).call(this);
  const useAttribute = attributeValue && attributeValue.startsWith("+") && (!inputValue || !inputValue.startsWith("+"));
  const value = useAttribute ? attributeValue : inputValue;
  const dialCode = __privateMethod(this, __Iti_instances, getDialCode_fn).call(this, value);
  const isRegionlessNanpNumber = isRegionlessNanp(value);
  const { initialCountry, geoIpLookup } = __privateGet(this, _options2);
  const isAutoCountry = initialCountry === INITIAL_COUNTRY.AUTO && geoIpLookup;
  const resolvedInitialCountry = isAutoCountry && intlTelInput.autoCountry ? intlTelInput.autoCountry : initialCountry;
  const doingAutoCountryLookup = isAutoCountry && !overrideAutoCountry && !intlTelInput.autoCountry;
  const isValidInitialCountry = isIso2(resolvedInitialCountry);
  if (dialCode) {
    if (isRegionlessNanpNumber) {
      if (isValidInitialCountry) {
        __privateMethod(this, __Iti_instances, updateSelectedCountry_fn).call(this, resolvedInitialCountry);
      } else if (!doingAutoCountryLookup) {
        __privateMethod(this, __Iti_instances, updateSelectedCountry_fn).call(this, US.ISO2);
      }
    } else {
      if (isValidInitialCountry) {
        __privateMethod(this, __Iti_instances, updateSelectedCountry_fn).call(this, resolvedInitialCountry);
      }
      __privateMethod(this, __Iti_instances, updateCountryFromNumber_fn).call(this, value);
    }
  } else if (isValidInitialCountry) {
    __privateMethod(this, __Iti_instances, updateSelectedCountry_fn).call(this, resolvedInitialCountry);
  } else if (!doingAutoCountryLookup) {
    __privateMethod(this, __Iti_instances, updateSelectedCountry_fn).call(this, "");
  }
  if (value) {
    __privateMethod(this, __Iti_instances, updateValueFromNumber_fn).call(this, value);
  }
}, //* Initialise the main event listeners: input keyup, and click selected country.
initListeners_fn = function() {
  __privateMethod(this, __Iti_instances, bindAllTelInputListeners_fn).call(this);
  if (__privateGet(this, _options2).allowDropdown) {
    __privateGet(this, _ui).bindAllInitialDropdownListeners(
      __privateGet(this, _abortController).signal,
      () => __privateMethod(this, __Iti_instances, openDropdown_fn).call(this),
      () => __privateMethod(this, __Iti_instances, closeDropdown_fn).call(this)
    );
  }
  __privateGet(this, _ui).bindHiddenInputSubmitListener(
    __privateGet(this, _abortController).signal,
    () => this.getNumber(),
    () => {
      var _a4;
      return ((_a4 = __privateGet(this, _selectedCountry)) == null ? void 0 : _a4.iso2) || "";
    }
  );
}, //* Init requests: utils script / geo ip lookup.
startAsyncLoads_fn = function() {
  if (__privateGet(this, _utilsDeferred)) {
    const { loadUtils } = __privateGet(this, _options2);
    const doAttachUtils = () => {
      var _a4;
      (_a4 = intlTelInput.attachUtils(loadUtils)) == null ? void 0 : _a4.catch(() => {
      });
    };
    if (intlTelInput.documentReady()) {
      doAttachUtils();
    } else {
      window.addEventListener("load", doAttachUtils, {
        signal: __privateGet(this, _abortController).signal
      });
    }
  }
  if (__privateGet(this, _autoCountryDeferred)) {
    if (__privateGet(this, _selectedCountry)) {
      __privateGet(this, _autoCountryDeferred).resolve();
    } else {
      __privateMethod(this, __Iti_instances, loadAutoCountry_fn).call(this);
    }
  }
}, //* Perform the geo ip lookup.
loadAutoCountry_fn = function() {
  if (intlTelInput.autoCountry) {
    __privateMethod(this, __Iti_instances, handleAutoCountryLoaded_fn).call(this);
    return;
  }
  __privateGet(this, _ui).setLoading(true);
  if (intlTelInput.startedLoadingAutoCountry) {
    return;
  }
  intlTelInput.startedLoadingAutoCountry = true;
  if (typeof __privateGet(this, _options2).geoIpLookup === "function") {
    const successCallback = (iso2 = "") => {
      const iso2Lower = iso2.toLowerCase();
      if (isIso2(iso2Lower)) {
        intlTelInput.autoCountry = iso2Lower;
        setTimeout(() => _a3.forEachInstance("handleAutoCountryLoaded"));
      } else {
        _a3.forEachInstance("handleAutoCountryFailure");
      }
    };
    const failureCallback = () => {
      _a3.forEachInstance("handleAutoCountryFailure");
    };
    __privateGet(this, _options2).geoIpLookup(successCallback, failureCallback);
  }
}, openDropdownWithPlus_fn = function() {
  __privateMethod(this, __Iti_instances, openDropdown_fn).call(this);
  __privateGet(this, _ui).prefillSearchWithPlus();
}, //* Delete the character just typed (the one immediately before the caret). Used by Android workarounds where we can't preventDefault on keydown.
removeJustTypedChar_fn = function(inputValue) {
  const currentCaretPos = __privateGet(this, _ui).telInputEl.selectionStart || 0;
  const valueBeforeCaret = inputValue.substring(0, currentCaretPos - 1);
  const valueAfterCaret = inputValue.substring(currentCaretPos);
  __privateMethod(this, __Iti_instances, setTelInputValue_fn).call(this, valueBeforeCaret + valueAfterCaret);
  return currentCaretPos - 1;
}, //* Initialize the tel input listeners.
bindAllTelInputListeners_fn = function() {
  __privateMethod(this, __Iti_instances, bindInputListener_fn).call(this);
  __privateMethod(this, __Iti_instances, bindKeydownListener_fn).call(this);
  __privateMethod(this, __Iti_instances, bindPasteListener_fn).call(this);
}, //* Android workaround for handling plus when separateDialCode enabled (as impossible to handle with keydown/keyup, for which e.key always returns "Unidentified", see https://stackoverflow.com/q/59584061/217866)
handleAndroidPlusKey_fn = function(inputValue) {
  __privateMethod(this, __Iti_instances, removeJustTypedChar_fn).call(this, inputValue);
  __privateMethod(this, __Iti_instances, openDropdownWithPlus_fn).call(this);
}, //* Android strictMode workaround: the keydown-based filter can't block these because e.key is "Unidentified" on Android virtual keyboards, so strip them here on input.
handleAndroidStrictReject_fn = function(inputValue, rejectedInput) {
  const newCaretPos = __privateMethod(this, __Iti_instances, removeJustTypedChar_fn).call(this, inputValue);
  __privateGet(this, _ui).telInputEl.setSelectionRange(newCaretPos, newCaretPos);
  __privateMethod(this, __Iti_instances, dispatchEvent_fn).call(this, EVENTS.STRICT_REJECT, {
    source: "key",
    rejectedInput,
    reason: "invalid"
  });
}, //* Format the input value using libphonenumber's AYT formatter, preserving caret position (called after an input event).
formatAsYouType_fn = function(inputValue, isDeleteForwards) {
  const currentCaretPos = __privateGet(this, _ui).telInputEl.selectionStart || 0;
  const valueBeforeCaret = inputValue.substring(0, currentCaretPos);
  const relevantCharsBeforeCaret = valueBeforeCaret.replace(
    REGEX.NON_PLUS_NUMERIC_GLOBAL,
    ""
  ).length;
  const fullNumber = __privateMethod(this, __Iti_instances, getFullNumber_fn).call(this);
  const formattedValue = formatNumberAsYouType(
    fullNumber,
    inputValue,
    intlTelInput.utils,
    __privateGet(this, _selectedCountry),
    __privateGet(this, _options2).separateDialCode
  );
  const newCaretPos = computeNewCaretPosition(
    relevantCharsBeforeCaret,
    formattedValue,
    currentCaretPos,
    isDeleteForwards
  );
  __privateMethod(this, __Iti_instances, setTelInputValue_fn).call(this, formattedValue);
  __privateGet(this, _ui).telInputEl.setSelectionRange(newCaretPos, newCaretPos);
}, //* If separateDialCode AND typed dial code (e.g. from paste or autofill, or from typing a dial code when countrySearch disabled), then remove the typed dial code.
//* Only strip when a full dial code is actually present — otherwise a lone typed "+" (or partial prefix) would get erased.
stripTypedDialCode_fn = function(inputValue) {
  if (inputValue.startsWith("+") && __privateGet(this, _selectedCountry) && __privateMethod(this, __Iti_instances, getDialCode_fn).call(this, inputValue)) {
    const cleanNumber = stripSeparateDialCode(
      inputValue,
      true,
      true,
      __privateGet(this, _selectedCountry)
    );
    __privateMethod(this, __Iti_instances, setTelInputValue_fn).call(this, cleanNumber);
  }
}, bindInputListener_fn = function() {
  __privateSet(this, _userOverrideFormatting, REGEX.ALPHA_UNICODE.test(
    __privateMethod(this, __Iti_instances, getTelInputValue_fn).call(this)
  ));
  __privateGet(this, _ui).telInputEl.addEventListener(
    "input",
    __privateGet(this, _handleInputEvent),
    {
      signal: __privateGet(this, _abortController).signal
    }
  );
}, _handleInputEvent = new WeakMap(), bindKeydownListener_fn = function() {
  const { strictMode, separateDialCode } = __privateGet(this, _options2);
  if (!strictMode && !separateDialCode) {
    return;
  }
  __privateGet(this, _ui).telInputEl.addEventListener("keydown", __privateGet(this, _handleKeydownEvent), {
    signal: __privateGet(this, _abortController).signal
  });
}, _handleKeydownEvent = new WeakMap(), bindPasteListener_fn = function() {
  if (!__privateGet(this, _options2).strictMode) {
    return;
  }
  __privateGet(this, _ui).telInputEl.addEventListener("paste", __privateGet(this, _handlePasteEvent), {
    signal: __privateGet(this, _abortController).signal
  });
}, _handlePasteEvent = new WeakMap(), //* Adhere to the input's maxlength attr.
truncateToMaxLength_fn = function(number) {
  const max = Number(__privateGet(this, _ui).telInputEl.getAttribute("maxlength"));
  return max && number.length > max ? number.substring(0, max) : number;
}, //* Trigger a custom event on the input (typed via ItiEventMap).
dispatchEvent_fn = function(name, detailProps = {}) {
  const e = new CustomEvent(name, {
    bubbles: true,
    cancelable: true,
    detail: detailProps
  });
  __privateGet(this, _ui).telInputEl.dispatchEvent(e);
}, //* Open the dropdown.
openDropdown_fn = function() {
  __privateGet(this, _ui).openDropdown(
    (li) => __privateMethod(this, __Iti_instances, selectListItem_fn).call(this, li),
    () => __privateMethod(this, __Iti_instances, closeDropdown_fn).call(this)
  );
  __privateMethod(this, __Iti_instances, dispatchEvent_fn).call(this, EVENTS.OPEN_COUNTRY_DROPDOWN);
}, //* Update the input's value to the given number (format first if possible)
//* NOTE: this is called from setInitialState, handleUtilsLoaded and setNumber.
updateValueFromNumber_fn = function(fullNumber) {
  var _a4;
  const { formatOnDisplay, nationalMode, separateDialCode } = __privateGet(this, _options2);
  let number = fullNumber;
  if (formatOnDisplay && intlTelInput.utils && __privateGet(this, _selectedCountry)) {
    const isRegionless = hasRegionlessDialCode(fullNumber);
    const useNational = nationalMode && !isRegionless || !number.startsWith("+") && !separateDialCode;
    const { NATIONAL, INTERNATIONAL } = intlTelInput.utils.numberFormat;
    const format = useNational ? NATIONAL : INTERNATIONAL;
    number = intlTelInput.utils.formatNumber(
      number,
      (_a4 = __privateGet(this, _selectedCountry)) == null ? void 0 : _a4.iso2,
      format
    );
  }
  number = __privateMethod(this, __Iti_instances, prepareNumberForInput_fn).call(this, number);
  __privateMethod(this, __Iti_instances, setTelInputValue_fn).call(this, number);
}, //* Check if need to select a new country based on the given number
//* Note: called from setInitialState, keyup handler, setNumber.
updateCountryFromNumber_fn = function(fullNumber) {
  const iso2 = __privateMethod(this, __Iti_instances, resolveCountryChangeFromNumber_fn).call(this, fullNumber);
  if (iso2 !== null) {
    return __privateMethod(this, __Iti_instances, updateSelectedCountry_fn).call(this, iso2);
  }
  return false;
}, // if there is a selected country, and the number doesn't start with a dial code, then add it
withDialCodePrefix_fn = function(number) {
  var _a4, _b;
  const dialCode = (_a4 = __privateGet(this, _selectedCountry)) == null ? void 0 : _a4.dialCode;
  const nationalPrefix = (_b = __privateGet(this, _selectedCountry)) == null ? void 0 : _b.nationalPrefix;
  const alreadyHasPlus = number.startsWith("+");
  if (alreadyHasPlus || !dialCode) {
    return number;
  }
  const hasPrefix = nationalPrefix && number.startsWith(nationalPrefix) && !__privateGet(this, _options2).separateDialCode;
  const cleanNumber = hasPrefix ? number.substring(1) : number;
  return `+${dialCode}${cleanNumber}`;
}, //* Get the new country iso2 (or "" for empty/globe state) based on the input number, or return null if no change.
resolveCountryChangeFromNumber_fn = function(fullNumber) {
  var _a4, _b;
  const plusIndex = fullNumber.indexOf("+");
  let number = plusIndex > 0 ? fullNumber.substring(plusIndex) : fullNumber;
  const selectedIso2 = (_a4 = __privateGet(this, _selectedCountry)) == null ? void 0 : _a4.iso2;
  number = __privateMethod(this, __Iti_instances, withDialCodePrefix_fn).call(this, number);
  const dialCodeMatch = __privateMethod(this, __Iti_instances, getDialCode_fn).call(this, number, true);
  const numeric = getNumeric(number);
  if (dialCodeMatch) {
    const dialCodeMatchNumeric = getNumeric(dialCodeMatch);
    const iso2Codes = __privateGet(this, _dialCodeToIso2Map)[dialCodeMatchNumeric];
    if (iso2Codes.length === 1) {
      if (iso2Codes[0] === selectedIso2) {
        return null;
      }
      return iso2Codes[0];
    }
    return __privateMethod(this, __Iti_instances, resolveCountryChangeFromMultiMatch_fn).call(this, iso2Codes, dialCodeMatchNumeric, numeric);
  } else if (number.startsWith("+") && numeric.length) {
    const currentDial = ((_b = __privateGet(this, _selectedCountry)) == null ? void 0 : _b.dialCode) || "";
    if (currentDial && currentDial.startsWith(numeric)) {
      return null;
    }
    return "";
  } else if ((!number || number === "+") && !selectedIso2 && __privateGet(this, _fallbackCountryIso2)) {
    return __privateGet(this, _fallbackCountryIso2);
  }
  return null;
}, //* Resolve the country when multiple countries share the matched dial code.
resolveCountryChangeFromMultiMatch_fn = function(iso2Codes, dialCodeMatchNumeric, numeric) {
  var _a4, _b, _c, _d;
  const selectedIso2 = (_a4 = __privateGet(this, _selectedCountry)) == null ? void 0 : _a4.iso2;
  const selectedDialCode = (_b = __privateGet(this, _selectedCountry)) == null ? void 0 : _b.dialCode;
  if (!selectedIso2 && __privateGet(this, _fallbackCountryIso2) && iso2Codes.includes(__privateGet(this, _fallbackCountryIso2))) {
    return __privateGet(this, _fallbackCountryIso2);
  }
  const isRegionlessNanpNumber = selectedDialCode === DIAL_CODE.NANP && isRegionlessNanp(numeric);
  if (isRegionlessNanpNumber) {
    return null;
  }
  const areaCodes = (_c = __privateGet(this, _selectedCountry)) == null ? void 0 : _c.areaCodes;
  const priority = (_d = __privateGet(this, _selectedCountry)) == null ? void 0 : _d.priority;
  if (areaCodes) {
    const dialCodeAreaCodes = areaCodes.map(
      (areaCode) => `${selectedDialCode}${areaCode}`
    );
    for (const dialCodeAreaCode of dialCodeAreaCodes) {
      if (numeric.startsWith(dialCodeAreaCode)) {
        return null;
      }
    }
  }
  const isMainCountry = priority === 0;
  const hasAreaCodesButNoneMatched = areaCodes && !isMainCountry && numeric.length > dialCodeMatchNumeric.length;
  const isValidSelection = selectedIso2 && iso2Codes.includes(selectedIso2) && !hasAreaCodesButNoneMatched;
  const alreadySelected = selectedIso2 === iso2Codes[0];
  if (!isValidSelection && !alreadySelected) {
    return iso2Codes[0];
  }
  return null;
}, //* Update the selected country, dial code (if separateDialCode), placeholder, title, and selected list item.
//* Note: called from setInitialState, updateCountryFromNumber, selectListItem, setCountry.
updateSelectedCountry_fn = function(iso2) {
  var _a4;
  const prevIso2 = ((_a4 = __privateGet(this, _selectedCountry)) == null ? void 0 : _a4.iso2) || "";
  __privateSet(this, _selectedCountry, iso2 ? __privateGet(this, _countryByIso2).get(iso2) : null);
  if (__privateGet(this, _selectedCountry)) {
    __privateSet(this, _fallbackCountryIso2, __privateGet(this, _selectedCountry).iso2);
  }
  __privateGet(this, _ui).setCountry(__privateGet(this, _selectedCountry));
  __privateMethod(this, __Iti_instances, updatePlaceholder_fn).call(this);
  __privateMethod(this, __Iti_instances, updateMaxCoreNumberLength_fn).call(this);
  return prevIso2 !== iso2;
}, //* Update the maximum valid number length for the currently selected country.
updateMaxCoreNumberLength_fn = function() {
  var _a4;
  const { strictMode, placeholderNumberType, allowedNumberTypes } = __privateGet(this, _options2);
  if (!strictMode || !intlTelInput.utils) {
    return;
  }
  const iso2 = (_a4 = __privateGet(this, _selectedCountry)) == null ? void 0 : _a4.iso2;
  if (!iso2) {
    __privateSet(this, _maxCoreNumberLength, null);
    return;
  }
  const numberType = intlTelInput.utils.numberType[placeholderNumberType];
  let exampleNumber = intlTelInput.utils.getExampleNumber(
    iso2,
    false,
    numberType,
    true
  );
  let validNumber = exampleNumber;
  while (intlTelInput.utils.isPossibleNumber(
    exampleNumber,
    iso2,
    allowedNumberTypes
  )) {
    validNumber = exampleNumber;
    exampleNumber += "0";
  }
  const coreNumber = intlTelInput.utils.getCoreNumber(validNumber, iso2);
  __privateSet(this, _maxCoreNumberLength, coreNumber.length);
  if (iso2 === "by") {
    __privateSet(this, _maxCoreNumberLength, coreNumber.length + 1);
  }
}, //* Update the input placeholder to an example number from the currently selected country.
updatePlaceholder_fn = function() {
  const {
    autoPlaceholder,
    placeholderNumberType,
    nationalMode,
    customPlaceholder
  } = __privateGet(this, _options2);
  const shouldSetPlaceholder = autoPlaceholder === PLACEHOLDER_MODES.AGGRESSIVE || !__privateGet(this, _ui).hadInitialPlaceholder && autoPlaceholder === PLACEHOLDER_MODES.POLITE;
  if (!intlTelInput.utils || !shouldSetPlaceholder) {
    return;
  }
  const numberType = intlTelInput.utils.numberType[placeholderNumberType];
  let placeholder = __privateGet(this, _selectedCountry) ? intlTelInput.utils.getExampleNumber(
    __privateGet(this, _selectedCountry).iso2,
    nationalMode,
    numberType
  ) : "";
  placeholder = __privateMethod(this, __Iti_instances, prepareNumberForInput_fn).call(this, placeholder);
  if (typeof customPlaceholder === "function") {
    placeholder = customPlaceholder(placeholder, __privateGet(this, _selectedCountry));
  }
  __privateGet(this, _ui).telInputEl.setAttribute("placeholder", placeholder);
}, //* Called when the user selects a list item from the dropdown (no-op if listItem is null).
selectListItem_fn = function(listItem) {
  if (!listItem) {
    return;
  }
  const iso2 = listItem.dataset[DATA_KEYS.ISO2];
  const countryChanged = __privateMethod(this, __Iti_instances, updateSelectedCountry_fn).call(this, iso2);
  __privateMethod(this, __Iti_instances, closeDropdown_fn).call(this);
  const dialCode = listItem.dataset[DATA_KEYS.DIAL_CODE];
  __privateMethod(this, __Iti_instances, updateDialCode_fn).call(this, dialCode);
  if (__privateGet(this, _options2).formatOnDisplay) {
    const inputValue = __privateMethod(this, __Iti_instances, getTelInputValue_fn).call(this);
    __privateMethod(this, __Iti_instances, updateValueFromNumber_fn).call(this, inputValue);
  }
  __privateGet(this, _ui).telInputEl.focus();
  if (countryChanged) {
    __privateMethod(this, __Iti_instances, dispatchCountryChangeEvent_fn).call(this);
    __privateMethod(this, __Iti_instances, dispatchEvent_fn).call(this, EVENTS.INPUT, { isCountryChange: true });
  }
}, //* Close the dropdown and unbind any listeners.
closeDropdown_fn = function(isDestroy) {
  if (!__privateGet(this, _ui).isDropdownOpen() || __privateGet(this, _options2).dropdownAlwaysOpen && !isDestroy) {
    return;
  }
  __privateGet(this, _ui).closeDropdown();
  __privateMethod(this, __Iti_instances, dispatchEvent_fn).call(this, EVENTS.CLOSE_COUNTRY_DROPDOWN);
}, //* Replace any existing dial code with the new one
//* Note: called from selectListItem and setCountry
updateDialCode_fn = function(newDialCodeDigits) {
  const inputValue = __privateMethod(this, __Iti_instances, getTelInputValue_fn).call(this);
  if (!inputValue.startsWith("+")) {
    return;
  }
  const newDialCode = `+${newDialCodeDigits}`;
  const prevDialCode = __privateMethod(this, __Iti_instances, getDialCode_fn).call(this, inputValue);
  let newNumber;
  if (prevDialCode) {
    newNumber = inputValue.replace(prevDialCode, newDialCode);
  } else {
    newNumber = newDialCode;
  }
  __privateMethod(this, __Iti_instances, setTelInputValue_fn).call(this, newNumber);
}, //* Try and extract a valid international dial code from a full telephone number.
//* Note: returns the raw string inc plus character and any whitespace/dots etc.
getDialCode_fn = function(number, includeAreaCode) {
  if (!number.startsWith("+")) {
    return "";
  }
  let dialCode = "";
  let numericChars = "";
  let foundBaseDialCode = false;
  for (let i = 0; i < number.length; i++) {
    const c = number.charAt(i);
    if (!/[0-9]/.test(c)) {
      continue;
    }
    numericChars += c;
    const hasMapEntry = Boolean(__privateGet(this, _dialCodeToIso2Map)[numericChars]);
    if (!hasMapEntry) {
      break;
    }
    if (__privateGet(this, _dialCodes).has(numericChars)) {
      dialCode = number.substring(0, i + 1);
      foundBaseDialCode = true;
      if (!includeAreaCode) {
        break;
      }
    } else if (includeAreaCode && foundBaseDialCode) {
      dialCode = number.substring(0, i + 1);
    }
    if (numericChars.length === __privateGet(this, _dialCodeMaxLength)) {
      break;
    }
  }
  return dialCode;
}, //* Build a full number from an already-normalised value, adding the dial code if separateDialCode is enabled.
buildFullNumber_fn = function(value) {
  var _a4;
  const dialCode = (_a4 = __privateGet(this, _selectedCountry)) == null ? void 0 : _a4.dialCode;
  const numericValue = getNumeric(value);
  const usePrefix = __privateGet(this, _options2).separateDialCode && !value.startsWith("+") && dialCode && numericValue;
  return (usePrefix ? `+${dialCode}` : "") + value;
}, //* Get the input value as a full number, adding the dial code if separateDialCode is enabled.
getFullNumber_fn = function() {
  const value = __privateMethod(this, __Iti_instances, getTelInputValue_fn).call(this);
  return __privateMethod(this, __Iti_instances, buildFullNumber_fn).call(this, value);
}, //* Remove the dial code if separateDialCode is enabled also cap the length if the input has a maxlength attribute
prepareNumberForInput_fn = function(fullNumber) {
  const hasValidDialCode = Boolean(__privateMethod(this, __Iti_instances, getDialCode_fn).call(this, fullNumber));
  const number = stripSeparateDialCode(
    fullNumber,
    hasValidDialCode,
    __privateGet(this, _options2).separateDialCode,
    __privateGet(this, _selectedCountry)
  );
  return __privateMethod(this, __Iti_instances, truncateToMaxLength_fn).call(this, number);
}, //* Return only the public-facing subset of the selected country data.
getPublicCountryData_fn = function() {
  const d = __privateGet(this, _selectedCountry);
  if (!d) {
    return null;
  }
  const { iso2, dialCode, name } = d;
  return { iso2, dialCode, name };
}, //* Dispatch the 'countrychange' event.
dispatchCountryChangeEvent_fn = function() {
  const countryData = __privateMethod(this, __Iti_instances, getPublicCountryData_fn).call(this);
  __privateMethod(this, __Iti_instances, dispatchEvent_fn).call(this, EVENTS.COUNTRY_CHANGE, countryData);
}, //**************************
//*  INTERNAL METHODS
//**************************
//* Called when the geoip call returns.
handleAutoCountryLoaded_fn = function() {
  if (!__privateGet(this, _autoCountryDeferred) || !intlTelInput.autoCountry) {
    return;
  }
  if (!__privateGet(this, _isActive)) {
    __privateGet(this, _autoCountryDeferred).resolve();
    return;
  }
  if (__privateGet(this, _ui).isLoading()) {
    this.setCountry(intlTelInput.autoCountry);
  } else {
    __privateSet(this, _fallbackCountryIso2, intlTelInput.autoCountry);
  }
  __privateGet(this, _ui).setLoading(false);
  __privateGet(this, _autoCountryDeferred).resolve();
}, //* Called when the geoip call fails or times out.
handleAutoCountryFailure_fn = function() {
  var _a4, _b;
  if (!__privateGet(this, _isActive)) {
    (_a4 = __privateGet(this, _autoCountryDeferred)) == null ? void 0 : _a4.reject();
    return;
  }
  __privateMethod(this, __Iti_instances, setInitialState_fn).call(this, true);
  __privateGet(this, _ui).setLoading(false);
  (_b = __privateGet(this, _autoCountryDeferred)) == null ? void 0 : _b.reject();
}, //* Called when the utils request completes.
handleUtilsLoaded_fn = function() {
  var _a4, _b, _c;
  if (!__privateGet(this, _isActive)) {
    (_a4 = __privateGet(this, _utilsDeferred)) == null ? void 0 : _a4.resolve();
    return;
  }
  if (!intlTelInput.utils) {
    (_b = __privateGet(this, _utilsDeferred)) == null ? void 0 : _b.resolve();
    return;
  }
  const inputValue = __privateMethod(this, __Iti_instances, getTelInputValue_fn).call(this);
  if (inputValue) {
    __privateMethod(this, __Iti_instances, updateValueFromNumber_fn).call(this, inputValue);
  }
  if (__privateGet(this, _selectedCountry)) {
    __privateMethod(this, __Iti_instances, updatePlaceholder_fn).call(this);
    __privateMethod(this, __Iti_instances, updateMaxCoreNumberLength_fn).call(this);
  }
  (_c = __privateGet(this, _utilsDeferred)) == null ? void 0 : _c.resolve();
}, //* Called when the utils request fails or times out.
handleUtilsFailure_fn = function(error) {
  var _a4, _b;
  if (!__privateGet(this, _isActive)) {
    (_a4 = __privateGet(this, _utilsDeferred)) == null ? void 0 : _a4.reject(error);
    return;
  }
  (_b = __privateGet(this, _utilsDeferred)) == null ? void 0 : _b.reject(error);
}, //* Shared internal validation logic to handle alpha character extension rules.
validateNumber_fn = function(mode) {
  var _a4;
  const { allowNumberExtensions, allowPhonewords, allowedNumberTypes } = __privateGet(this, _options2);
  const iso2 = (_a4 = __privateGet(this, _selectedCountry)) == null ? void 0 : _a4.iso2;
  const value = __privateMethod(this, __Iti_instances, getFullNumber_fn).call(this);
  if (!__privateGet(this, _selectedCountry) && !hasRegionlessDialCode(value)) {
    return false;
  }
  const check = mode === "precise" ? intlTelInput.utils.isValidNumber : intlTelInput.utils.isPossibleNumber;
  if (!check(value, iso2, allowedNumberTypes)) {
    return false;
  }
  if (REGEX.ALPHA_UNICODE.test(value)) {
    const hasExtension = Boolean(
      intlTelInput.utils.getExtension(value, iso2)
    );
    return hasExtension ? allowNumberExtensions : allowPhonewords;
  }
  return true;
}, _a3);
var attachUtils = (source) => {
  if (!intlTelInput.utils && !intlTelInput.startedLoadingUtils) {
    let loadCall;
    if (typeof source === "function") {
      try {
        loadCall = Promise.resolve(source());
      } catch (error) {
        return Promise.reject(error);
      }
    } else {
      return Promise.reject(
        new TypeError(
          `The argument passed to attachUtils must be a function that returns a promise for the utils module, not ${typeof source}`
        )
      );
    }
    intlTelInput.startedLoadingUtils = true;
    return loadCall.then((module) => {
      const utils = module == null ? void 0 : module.default;
      if (!utils || typeof utils !== "object") {
        throw new TypeError(
          "The loader function passed to attachUtils did not resolve to a module object with utils as its default export."
        );
      }
      intlTelInput.utils = utils;
      Iti.forEachInstance("handleUtilsLoaded");
      return true;
    }).catch((error) => {
      Iti.forEachInstance("handleUtilsFailure", error);
      throw error;
    });
  }
  return null;
};
var intlTelInput = Object.assign(
  (input, options) => {
    const iti = new Iti(input, options);
    intlTelInput.instances.set(String(iti.id), iti);
    input.iti = iti;
    return iti;
  },
  {
    defaults,
    //* Using a static var like this allows us to mock it in the tests.
    documentReady: () => document.readyState === "complete",
    //* Get the country data object.
    getCountryData: () => data_default,
    //* A getter for the plugin instance.
    getInstance: (input) => {
      const id = input.dataset[DATA_KEYS.INSTANCE_ID];
      return id ? intlTelInput.instances.get(id) ?? null : null;
    },
    //* A map from instance ID to instance object.
    instances: /* @__PURE__ */ new Map(),
    attachUtils,
    startedLoadingUtils: false,
    startedLoadingAutoCountry: false,
    version: "27.1.3"
  }
);
var intlTelInput_default = intlTelInput;
export {
  Iti,
  intlTelInput_default as default
};
//# sourceMappingURL=intl-tel-input.js.map
